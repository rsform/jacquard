// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: sh.weaver.notebook.getEntry
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{CowStr, BosStr, DefaultStr, FromStaticStr};
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::string::AtUri;
use jacquard_common::types::value::Data;
use jacquard_derive::{IntoStatic, open_union};
use serde::{Serialize, Deserialize};
use crate::sh_weaver::notebook::EntryView;

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct GetEntry<S: BosStr = DefaultStr> {
    pub uri: AtUri<S>,
}


#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct GetEntryOutput<S: BosStr = DefaultStr> {
    #[serde(flatten)]
    pub value: EntryView<S>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}


#[derive(
    Serialize,
    Deserialize,
    Debug,
    Clone,
    PartialEq,
    Eq,
    thiserror::Error,
    miette::Diagnostic
)]

#[serde(tag = "error", content = "message")]
pub enum GetEntryError {
    #[serde(rename = "EntryNotFound")]
    EntryNotFound(Option<SmolStr>),
    /// Catch-all for unknown error codes.
    #[serde(untagged)]
    Other { error: SmolStr, message: Option<SmolStr> },
}

impl core::fmt::Display for GetEntryError {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::EntryNotFound(msg) => {
                write!(f, "EntryNotFound")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::Other { error, message } => {
                write!(f, "{}", error)?;
                if let Some(msg) = message {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
        }
    }
}

/** Response marker for the `sh.weaver.notebook.getEntry` query.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `GetEntryOutput<S>` for this endpoint.*/
pub struct GetEntryResponse;
impl jacquard_common::xrpc::XrpcResp for GetEntryResponse {
    const NSID: &'static str = "sh.weaver.notebook.getEntry";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = GetEntryOutput<S>;
    type Err = GetEntryError;
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for GetEntry<S> {
    const NSID: &'static str = "sh.weaver.notebook.getEntry";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Query;
    type Response = GetEntryResponse;
}

/** Endpoint marker for the `sh.weaver.notebook.getEntry` query.

Path: `/xrpc/sh.weaver.notebook.getEntry`. The request payload type is `GetEntry<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct GetEntryRequest;
impl jacquard_common::xrpc::XrpcEndpoint for GetEntryRequest {
    const PATH: &'static str = "/xrpc/sh.weaver.notebook.getEntry";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Query;
    type Request<S: BosStr> = GetEntry<S>;
    type Response = GetEntryResponse;
}

pub mod get_entry_state {

    pub use crate::builder_types::{Set, Unset, IsSet, IsUnset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Uri;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Uri = Unset;
    }
    ///State transition - sets the `uri` field to Set
    pub struct SetUri<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetUri<St> {}
    impl<St: State> State for SetUri<St> {
        type Uri = Set<members::uri>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `uri` field
        pub struct uri(());
    }
}

/// Builder for constructing an instance of this type.
pub struct GetEntryBuilder<St: get_entry_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<AtUri<S>>,),
    _type: PhantomData<fn() -> S>,
}

impl GetEntry<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> GetEntryBuilder<get_entry_state::Empty, DefaultStr> {
        GetEntryBuilder::new()
    }
}

impl<S: BosStr> GetEntry<S> {
    /// Create a new builder for this type
    pub fn builder() -> GetEntryBuilder<get_entry_state::Empty, S> {
        GetEntryBuilder::builder()
    }
}

impl GetEntryBuilder<get_entry_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        GetEntryBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> GetEntryBuilder<get_entry_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        GetEntryBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> GetEntryBuilder<St, S>
where
    St: get_entry_state::State,
    St::Uri: get_entry_state::IsUnset,
{
    /// Set the `uri` field (required)
    pub fn uri(
        mut self,
        value: impl Into<AtUri<S>>,
    ) -> GetEntryBuilder<get_entry_state::SetUri<St>, S> {
        self._fields.0 = Option::Some(value.into());
        GetEntryBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> GetEntryBuilder<St, S>
where
    St: get_entry_state::State,
    St::Uri: get_entry_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> GetEntry<S> {
        GetEntry {
            uri: self._fields.0.unwrap(),
        }
    }
}