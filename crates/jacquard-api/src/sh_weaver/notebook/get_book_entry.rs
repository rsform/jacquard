// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: sh.weaver.notebook.getBookEntry
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

use crate::sh_weaver::notebook::BookEntryView;
#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::string::AtUri;
use jacquard_common::types::value::Data;
use jacquard_common::{BosStr, CowStr, DefaultStr, FromStaticStr};
use jacquard_derive::{IntoStatic, open_union};
use serde::{Deserialize, Serialize};

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct GetBookEntry<S: BosStr = DefaultStr> {
    /// Defaults to `0`. Min: 0.
    #[serde(default = "_default_index")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub index: Option<i64>,
    pub notebook: AtUri<S>,
}

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct GetBookEntryOutput<S: BosStr = DefaultStr> {
    #[serde(flatten)]
    pub value: BookEntryView<S>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

#[derive(
    Serialize, Deserialize, Debug, Clone, PartialEq, Eq, thiserror::Error, miette::Diagnostic,
)]
#[serde(tag = "error", content = "message")]
pub enum GetBookEntryError {
    #[serde(rename = "NotebookNotFound")]
    NotebookNotFound(Option<SmolStr>),
    #[serde(rename = "EntryNotFound")]
    EntryNotFound(Option<SmolStr>),
    /// Catch-all for unknown error codes.
    #[serde(untagged)]
    Other {
        error: SmolStr,
        message: Option<SmolStr>,
    },
}

impl core::fmt::Display for GetBookEntryError {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::NotebookNotFound(msg) => {
                write!(f, "NotebookNotFound")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::EntryNotFound(msg) => {
                write!(f, "EntryNotFound")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::Other { error, message } => {
                write!(f, "{}", error)?;
                if let Some(msg) = message {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
        }
    }
}

/** Response marker for the `sh.weaver.notebook.getBookEntry` query.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `GetBookEntryOutput<S>` for this endpoint.*/
pub struct GetBookEntryResponse;
impl jacquard_common::xrpc::XrpcResp for GetBookEntryResponse {
    const NSID: &'static str = "sh.weaver.notebook.getBookEntry";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = GetBookEntryOutput<S>;
    type Err = GetBookEntryError;
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for GetBookEntry<S> {
    const NSID: &'static str = "sh.weaver.notebook.getBookEntry";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Query;
    type Response = GetBookEntryResponse;
}

/** Endpoint marker for the `sh.weaver.notebook.getBookEntry` query.

Path: `/xrpc/sh.weaver.notebook.getBookEntry`. The request payload type is `GetBookEntry<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct GetBookEntryRequest;
impl jacquard_common::xrpc::XrpcEndpoint for GetBookEntryRequest {
    const PATH: &'static str = "/xrpc/sh.weaver.notebook.getBookEntry";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Query;
    type Request<S: BosStr> = GetBookEntry<S>;
    type Response = GetBookEntryResponse;
}

fn _default_index() -> Option<i64> {
    Some(0i64)
}

pub mod get_book_entry_state {

    pub use crate::builder_types::{IsSet, IsUnset, Set, Unset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Notebook;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Notebook = Unset;
    }
    ///State transition - sets the `notebook` field to Set
    pub struct SetNotebook<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetNotebook<St> {}
    impl<St: State> State for SetNotebook<St> {
        type Notebook = Set<members::notebook>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `notebook` field
        pub struct notebook(());
    }
}

/// Builder for constructing an instance of this type.
pub struct GetBookEntryBuilder<St: get_book_entry_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<i64>, Option<AtUri<S>>),
    _type: PhantomData<fn() -> S>,
}

impl GetBookEntry<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> GetBookEntryBuilder<get_book_entry_state::Empty, DefaultStr> {
        GetBookEntryBuilder::new()
    }
}

impl<S: BosStr> GetBookEntry<S> {
    /// Create a new builder for this type
    pub fn builder() -> GetBookEntryBuilder<get_book_entry_state::Empty, S> {
        GetBookEntryBuilder::builder()
    }
}

impl GetBookEntryBuilder<get_book_entry_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        GetBookEntryBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> GetBookEntryBuilder<get_book_entry_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        GetBookEntryBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<St: get_book_entry_state::State, S: BosStr> GetBookEntryBuilder<St, S> {
    /// Set the `index` field (optional)
    pub fn index(mut self, value: impl Into<Option<i64>>) -> Self {
        self._fields.0 = value.into();
        self
    }
    /// Set the `index` field to an Option value (optional)
    pub fn maybe_index(mut self, value: Option<i64>) -> Self {
        self._fields.0 = value;
        self
    }
}

impl<St, S: BosStr> GetBookEntryBuilder<St, S>
where
    St: get_book_entry_state::State,
    St::Notebook: get_book_entry_state::IsUnset,
{
    /// Set the `notebook` field (required)
    pub fn notebook(
        mut self,
        value: impl Into<AtUri<S>>,
    ) -> GetBookEntryBuilder<get_book_entry_state::SetNotebook<St>, S> {
        self._fields.1 = Option::Some(value.into());
        GetBookEntryBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> GetBookEntryBuilder<St, S>
where
    St: get_book_entry_state::State,
    St::Notebook: get_book_entry_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> GetBookEntry<S> {
        GetBookEntry {
            index: self._fields.0,
            notebook: self._fields.1.unwrap(),
        }
    }
}
