// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: at.inlay.Binding
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{CowStr, BosStr, DefaultStr, FromStaticStr};
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::value::Data;
use jacquard_derive::IntoStatic;
use serde::{Serialize, Deserialize};
use crate::at_inlay::Response;

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct Binding<S: BosStr = DefaultStr> {
    ///Path segments to resolve against scope
    pub path: Vec<S>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}


#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct BindingOutput<S: BosStr = DefaultStr> {
    #[serde(flatten)]
    pub value: Response<S>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

/** Response marker for the `at.inlay.Binding` procedure.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `BindingOutput<S>` for this endpoint.*/
pub struct BindingResponse;
impl jacquard_common::xrpc::XrpcResp for BindingResponse {
    const NSID: &'static str = "at.inlay.Binding";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = BindingOutput<S>;
    type Err = jacquard_common::xrpc::GenericError;
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for Binding<S> {
    const NSID: &'static str = "at.inlay.Binding";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Procedure(
        "application/json",
    );
    type Response = BindingResponse;
}

/** Endpoint marker for the `at.inlay.Binding` procedure.

Path: `/xrpc/at.inlay.Binding`. The request payload type is `Binding<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct BindingRequest;
impl jacquard_common::xrpc::XrpcEndpoint for BindingRequest {
    const PATH: &'static str = "/xrpc/at.inlay.Binding";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Procedure(
        "application/json",
    );
    type Request<S: BosStr> = Binding<S>;
    type Response = BindingResponse;
}

pub mod binding_state {

    pub use crate::builder_types::{Set, Unset, IsSet, IsUnset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Path;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Path = Unset;
    }
    ///State transition - sets the `path` field to Set
    pub struct SetPath<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetPath<St> {}
    impl<St: State> State for SetPath<St> {
        type Path = Set<members::path>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `path` field
        pub struct path(());
    }
}

/// Builder for constructing an instance of this type.
pub struct BindingBuilder<St: binding_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<Vec<S>>,),
    _type: PhantomData<fn() -> S>,
}

impl Binding<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> BindingBuilder<binding_state::Empty, DefaultStr> {
        BindingBuilder::new()
    }
}

impl<S: BosStr> Binding<S> {
    /// Create a new builder for this type
    pub fn builder() -> BindingBuilder<binding_state::Empty, S> {
        BindingBuilder::builder()
    }
}

impl BindingBuilder<binding_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        BindingBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> BindingBuilder<binding_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        BindingBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> BindingBuilder<St, S>
where
    St: binding_state::State,
    St::Path: binding_state::IsUnset,
{
    /// Set the `path` field (required)
    pub fn path(
        mut self,
        value: impl Into<Vec<S>>,
    ) -> BindingBuilder<binding_state::SetPath<St>, S> {
        self._fields.0 = Option::Some(value.into());
        BindingBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> BindingBuilder<St, S>
where
    St: binding_state::State,
    St::Path: binding_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> Binding<S> {
        Binding {
            path: self._fields.0.unwrap(),
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(self, extra_data: BTreeMap<SmolStr, Data<S>>) -> Binding<S> {
        Binding {
            path: self._fields.0.unwrap(),
            extra_data: Some(extra_data),
        }
    }
}