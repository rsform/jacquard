// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: com.atproto.simplespace.getSpace
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{CowStr, BosStr, DefaultStr, FromStaticStr};
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::aturi::AtSpaceUri;
use jacquard_common::types::value::Data;
use jacquard_derive::{IntoStatic, open_union};
use serde::{Serialize, Deserialize};
use crate::com_atproto::simplespace::AllowList;
use crate::com_atproto::simplespace::ManagingAppPolicy;
use crate::com_atproto::simplespace::MemberListPolicy;
use crate::com_atproto::simplespace::Open;
use crate::com_atproto::simplespace::PublicPolicy;

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct GetSpace<S: BosStr = DefaultStr> {
    pub space: AtSpaceUri<S>,
}


#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct GetSpaceOutput<S: BosStr = DefaultStr> {
    ///How the authority decides whether to authorize a requesting app.
    pub app_access: GetSpaceOutputAppAccess<S>,
    ///How the authority decides whether to authorize a requesting user.
    pub policy: GetSpaceOutputPolicy<S>,
    ///URI of the space.
    pub uri: AtSpaceUri<S>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}


#[open_union]
#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(tag = "$type", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub enum GetSpaceOutputAppAccess<S: BosStr = DefaultStr> {
    #[serde(rename = "com.atproto.simplespace.defs#open")]
    Open(Box<Open<S>>),
    #[serde(rename = "com.atproto.simplespace.defs#allowList")]
    AllowList(Box<AllowList<S>>),
}


#[open_union]
#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(tag = "$type", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub enum GetSpaceOutputPolicy<S: BosStr = DefaultStr> {
    #[serde(rename = "com.atproto.simplespace.defs#publicPolicy")]
    PublicPolicy(Box<PublicPolicy<S>>),
    #[serde(rename = "com.atproto.simplespace.defs#memberListPolicy")]
    MemberListPolicy(Box<MemberListPolicy<S>>),
    #[serde(rename = "com.atproto.simplespace.defs#managingAppPolicy")]
    ManagingAppPolicy(Box<ManagingAppPolicy<S>>),
}


#[derive(
    Serialize,
    Deserialize,
    Debug,
    Clone,
    PartialEq,
    Eq,
    thiserror::Error,
    miette::Diagnostic
)]

#[serde(tag = "error", content = "message")]
pub enum GetSpaceError {
    #[serde(rename = "SpaceNotFound")]
    SpaceNotFound(Option<SmolStr>),
    /// Catch-all for unknown error codes.
    #[serde(untagged)]
    Other { error: SmolStr, message: Option<SmolStr> },
}

impl core::fmt::Display for GetSpaceError {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::SpaceNotFound(msg) => {
                write!(f, "SpaceNotFound")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::Other { error, message } => {
                write!(f, "{}", error)?;
                if let Some(msg) = message {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
        }
    }
}

/** Response marker for the `com.atproto.simplespace.getSpace` query.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `GetSpaceOutput<S>` for this endpoint.*/
pub struct GetSpaceResponse;
impl jacquard_common::xrpc::XrpcResp for GetSpaceResponse {
    const NSID: &'static str = "com.atproto.simplespace.getSpace";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = GetSpaceOutput<S>;
    type Err = GetSpaceError;
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for GetSpace<S> {
    const NSID: &'static str = "com.atproto.simplespace.getSpace";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Query;
    type Response = GetSpaceResponse;
}

/** Endpoint marker for the `com.atproto.simplespace.getSpace` query.

Path: `/xrpc/com.atproto.simplespace.getSpace`. The request payload type is `GetSpace<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct GetSpaceRequest;
impl jacquard_common::xrpc::XrpcEndpoint for GetSpaceRequest {
    const PATH: &'static str = "/xrpc/com.atproto.simplespace.getSpace";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Query;
    type Request<S: BosStr> = GetSpace<S>;
    type Response = GetSpaceResponse;
}

pub mod get_space_state {

    pub use crate::builder_types::{Set, Unset, IsSet, IsUnset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Space;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Space = Unset;
    }
    ///State transition - sets the `space` field to Set
    pub struct SetSpace<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetSpace<St> {}
    impl<St: State> State for SetSpace<St> {
        type Space = Set<members::space>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `space` field
        pub struct space(());
    }
}

/// Builder for constructing an instance of this type.
pub struct GetSpaceBuilder<St: get_space_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<AtSpaceUri<S>>,),
    _type: PhantomData<fn() -> S>,
}

impl GetSpace<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> GetSpaceBuilder<get_space_state::Empty, DefaultStr> {
        GetSpaceBuilder::new()
    }
}

impl<S: BosStr> GetSpace<S> {
    /// Create a new builder for this type
    pub fn builder() -> GetSpaceBuilder<get_space_state::Empty, S> {
        GetSpaceBuilder::builder()
    }
}

impl GetSpaceBuilder<get_space_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        GetSpaceBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> GetSpaceBuilder<get_space_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        GetSpaceBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> GetSpaceBuilder<St, S>
where
    St: get_space_state::State,
    St::Space: get_space_state::IsUnset,
{
    /// Set the `space` field (required)
    pub fn space(
        mut self,
        value: impl Into<AtSpaceUri<S>>,
    ) -> GetSpaceBuilder<get_space_state::SetSpace<St>, S> {
        self._fields.0 = Option::Some(value.into());
        GetSpaceBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> GetSpaceBuilder<St, S>
where
    St: get_space_state::State,
    St::Space: get_space_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> GetSpace<S> {
        GetSpace {
            space: self._fields.0.unwrap(),
        }
    }
}