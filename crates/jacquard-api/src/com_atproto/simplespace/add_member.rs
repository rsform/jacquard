// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: com.atproto.simplespace.addMember
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{CowStr, BosStr, DefaultStr, FromStaticStr};
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::aturi::AtSpaceUri;
use jacquard_common::types::string::Did;
use jacquard_common::types::value::Data;
use jacquard_derive::{IntoStatic, open_union};
use serde::{Serialize, Deserialize};

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct AddMember<S: BosStr = DefaultStr> {
    ///The DID of the member to add.
    pub did: Did<S>,
    ///Reference to the space.
    pub space: AtSpaceUri<S>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}


#[derive(
    Serialize,
    Deserialize,
    Debug,
    Clone,
    PartialEq,
    Eq,
    thiserror::Error,
    miette::Diagnostic
)]

#[serde(tag = "error", content = "message")]
pub enum AddMemberError {
    #[serde(rename = "SpaceNotFound")]
    SpaceNotFound(Option<SmolStr>),
    #[serde(rename = "NotSpaceOwner")]
    NotSpaceOwner(Option<SmolStr>),
    /// Catch-all for unknown error codes.
    #[serde(untagged)]
    Other { error: SmolStr, message: Option<SmolStr> },
}

impl core::fmt::Display for AddMemberError {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::SpaceNotFound(msg) => {
                write!(f, "SpaceNotFound")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::NotSpaceOwner(msg) => {
                write!(f, "NotSpaceOwner")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::Other { error, message } => {
                write!(f, "{}", error)?;
                if let Some(msg) = message {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
        }
    }
}

/** Response marker for the `com.atproto.simplespace.addMember` procedure.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `()` for this endpoint.*/
pub struct AddMemberResponse;
impl jacquard_common::xrpc::XrpcResp for AddMemberResponse {
    const NSID: &'static str = "com.atproto.simplespace.addMember";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = ();
    type Err = AddMemberError;
    fn decode_output<'de, S>(
        body: &'de [u8],
    ) -> Result<Self::Output<S>, jacquard_common::error::DecodeError>
    where
        S: BosStr + Deserialize<'de>,
    {
        if body.is_empty() {
            Ok(())
        } else {
            Err(jacquard_common::error::DecodeError::UnexpectedBody)
        }
    }
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for AddMember<S> {
    const NSID: &'static str = "com.atproto.simplespace.addMember";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Procedure(
        "application/json",
    );
    type Response = AddMemberResponse;
}

/** Endpoint marker for the `com.atproto.simplespace.addMember` procedure.

Path: `/xrpc/com.atproto.simplespace.addMember`. The request payload type is `AddMember<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct AddMemberRequest;
impl jacquard_common::xrpc::XrpcEndpoint for AddMemberRequest {
    const PATH: &'static str = "/xrpc/com.atproto.simplespace.addMember";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Procedure(
        "application/json",
    );
    type Request<S: BosStr> = AddMember<S>;
    type Response = AddMemberResponse;
}

pub mod add_member_state {

    pub use crate::builder_types::{Set, Unset, IsSet, IsUnset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Did;
        type Space;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Did = Unset;
        type Space = Unset;
    }
    ///State transition - sets the `did` field to Set
    pub struct SetDid<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetDid<St> {}
    impl<St: State> State for SetDid<St> {
        type Did = Set<members::did>;
        type Space = St::Space;
    }
    ///State transition - sets the `space` field to Set
    pub struct SetSpace<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetSpace<St> {}
    impl<St: State> State for SetSpace<St> {
        type Did = St::Did;
        type Space = Set<members::space>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `did` field
        pub struct did(());
        ///Marker type for the `space` field
        pub struct space(());
    }
}

/// Builder for constructing an instance of this type.
pub struct AddMemberBuilder<St: add_member_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<Did<S>>, Option<AtSpaceUri<S>>),
    _type: PhantomData<fn() -> S>,
}

impl AddMember<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> AddMemberBuilder<add_member_state::Empty, DefaultStr> {
        AddMemberBuilder::new()
    }
}

impl<S: BosStr> AddMember<S> {
    /// Create a new builder for this type
    pub fn builder() -> AddMemberBuilder<add_member_state::Empty, S> {
        AddMemberBuilder::builder()
    }
}

impl AddMemberBuilder<add_member_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        AddMemberBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> AddMemberBuilder<add_member_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        AddMemberBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> AddMemberBuilder<St, S>
where
    St: add_member_state::State,
    St::Did: add_member_state::IsUnset,
{
    /// Set the `did` field (required)
    pub fn did(
        mut self,
        value: impl Into<Did<S>>,
    ) -> AddMemberBuilder<add_member_state::SetDid<St>, S> {
        self._fields.0 = Option::Some(value.into());
        AddMemberBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> AddMemberBuilder<St, S>
where
    St: add_member_state::State,
    St::Space: add_member_state::IsUnset,
{
    /// Set the `space` field (required)
    pub fn space(
        mut self,
        value: impl Into<AtSpaceUri<S>>,
    ) -> AddMemberBuilder<add_member_state::SetSpace<St>, S> {
        self._fields.1 = Option::Some(value.into());
        AddMemberBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> AddMemberBuilder<St, S>
where
    St: add_member_state::State,
    St::Did: add_member_state::IsSet,
    St::Space: add_member_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> AddMember<S> {
        AddMember {
            did: self._fields.0.unwrap(),
            space: self._fields.1.unwrap(),
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(
        self,
        extra_data: BTreeMap<SmolStr, Data<S>>,
    ) -> AddMember<S> {
        AddMember {
            did: self._fields.0.unwrap(),
            space: self._fields.1.unwrap(),
            extra_data: Some(extra_data),
        }
    }
}