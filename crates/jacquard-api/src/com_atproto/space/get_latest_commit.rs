// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: com.atproto.space.getLatestCommit
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{CowStr, BosStr, DefaultStr, FromStaticStr};
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::aturi::AtSpaceUri;
use jacquard_common::types::string::Did;
use jacquard_common::types::value::Data;
use jacquard_derive::{IntoStatic, open_union};
use serde::{Serialize, Deserialize};
use crate::com_atproto::space::SignedCommit;

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct GetLatestCommit<S: BosStr = DefaultStr> {
    pub repo: Did<S>,
    pub space: AtSpaceUri<S>,
}


#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct GetLatestCommitOutput<S: BosStr = DefaultStr> {
    ///The account's current signed commit.
    pub commit: SignedCommit<S>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}


#[derive(
    Serialize,
    Deserialize,
    Debug,
    Clone,
    PartialEq,
    Eq,
    thiserror::Error,
    miette::Diagnostic
)]

#[serde(tag = "error", content = "message")]
pub enum GetLatestCommitError {
    #[serde(rename = "SpaceNotFound")]
    SpaceNotFound(Option<SmolStr>),
    #[serde(rename = "RepoNotFound")]
    RepoNotFound(Option<SmolStr>),
    #[serde(rename = "RepoTakendown")]
    RepoTakendown(Option<SmolStr>),
    #[serde(rename = "RepoSuspended")]
    RepoSuspended(Option<SmolStr>),
    #[serde(rename = "RepoDeactivated")]
    RepoDeactivated(Option<SmolStr>),
    /// Catch-all for unknown error codes.
    #[serde(untagged)]
    Other { error: SmolStr, message: Option<SmolStr> },
}

impl core::fmt::Display for GetLatestCommitError {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::SpaceNotFound(msg) => {
                write!(f, "SpaceNotFound")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::RepoNotFound(msg) => {
                write!(f, "RepoNotFound")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::RepoTakendown(msg) => {
                write!(f, "RepoTakendown")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::RepoSuspended(msg) => {
                write!(f, "RepoSuspended")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::RepoDeactivated(msg) => {
                write!(f, "RepoDeactivated")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::Other { error, message } => {
                write!(f, "{}", error)?;
                if let Some(msg) = message {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
        }
    }
}

/** Response marker for the `com.atproto.space.getLatestCommit` query.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `GetLatestCommitOutput<S>` for this endpoint.*/
pub struct GetLatestCommitResponse;
impl jacquard_common::xrpc::XrpcResp for GetLatestCommitResponse {
    const NSID: &'static str = "com.atproto.space.getLatestCommit";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = GetLatestCommitOutput<S>;
    type Err = GetLatestCommitError;
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for GetLatestCommit<S> {
    const NSID: &'static str = "com.atproto.space.getLatestCommit";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Query;
    type Response = GetLatestCommitResponse;
}

/** Endpoint marker for the `com.atproto.space.getLatestCommit` query.

Path: `/xrpc/com.atproto.space.getLatestCommit`. The request payload type is `GetLatestCommit<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct GetLatestCommitRequest;
impl jacquard_common::xrpc::XrpcEndpoint for GetLatestCommitRequest {
    const PATH: &'static str = "/xrpc/com.atproto.space.getLatestCommit";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Query;
    type Request<S: BosStr> = GetLatestCommit<S>;
    type Response = GetLatestCommitResponse;
}

pub mod get_latest_commit_state {

    pub use crate::builder_types::{Set, Unset, IsSet, IsUnset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Repo;
        type Space;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Repo = Unset;
        type Space = Unset;
    }
    ///State transition - sets the `repo` field to Set
    pub struct SetRepo<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetRepo<St> {}
    impl<St: State> State for SetRepo<St> {
        type Repo = Set<members::repo>;
        type Space = St::Space;
    }
    ///State transition - sets the `space` field to Set
    pub struct SetSpace<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetSpace<St> {}
    impl<St: State> State for SetSpace<St> {
        type Repo = St::Repo;
        type Space = Set<members::space>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `repo` field
        pub struct repo(());
        ///Marker type for the `space` field
        pub struct space(());
    }
}

/// Builder for constructing an instance of this type.
pub struct GetLatestCommitBuilder<
    St: get_latest_commit_state::State,
    S: BosStr = DefaultStr,
> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<Did<S>>, Option<AtSpaceUri<S>>),
    _type: PhantomData<fn() -> S>,
}

impl GetLatestCommit<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> GetLatestCommitBuilder<get_latest_commit_state::Empty, DefaultStr> {
        GetLatestCommitBuilder::new()
    }
}

impl<S: BosStr> GetLatestCommit<S> {
    /// Create a new builder for this type
    pub fn builder() -> GetLatestCommitBuilder<get_latest_commit_state::Empty, S> {
        GetLatestCommitBuilder::builder()
    }
}

impl GetLatestCommitBuilder<get_latest_commit_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        GetLatestCommitBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> GetLatestCommitBuilder<get_latest_commit_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        GetLatestCommitBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> GetLatestCommitBuilder<St, S>
where
    St: get_latest_commit_state::State,
    St::Repo: get_latest_commit_state::IsUnset,
{
    /// Set the `repo` field (required)
    pub fn repo(
        mut self,
        value: impl Into<Did<S>>,
    ) -> GetLatestCommitBuilder<get_latest_commit_state::SetRepo<St>, S> {
        self._fields.0 = Option::Some(value.into());
        GetLatestCommitBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> GetLatestCommitBuilder<St, S>
where
    St: get_latest_commit_state::State,
    St::Space: get_latest_commit_state::IsUnset,
{
    /// Set the `space` field (required)
    pub fn space(
        mut self,
        value: impl Into<AtSpaceUri<S>>,
    ) -> GetLatestCommitBuilder<get_latest_commit_state::SetSpace<St>, S> {
        self._fields.1 = Option::Some(value.into());
        GetLatestCommitBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> GetLatestCommitBuilder<St, S>
where
    St: get_latest_commit_state::State,
    St::Repo: get_latest_commit_state::IsSet,
    St::Space: get_latest_commit_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> GetLatestCommit<S> {
        GetLatestCommit {
            repo: self._fields.0.unwrap(),
            space: self._fields.1.unwrap(),
        }
    }
}