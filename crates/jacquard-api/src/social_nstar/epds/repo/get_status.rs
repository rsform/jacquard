// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: social.nstar.epds.repo.getStatus
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{CowStr, BosStr, DefaultStr, FromStaticStr};
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::string::{Did, Tid};
use jacquard_common::types::value::Data;
use jacquard_derive::IntoStatic;
use serde::{Serialize, Deserialize};

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct GetStatus<S: BosStr = DefaultStr> {
    pub did: Did<S>,
}


#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct GetStatusOutput<S: BosStr = DefaultStr> {
    pub active: bool,
    pub did: Did<S>,
    ///Optional field, the current rev of the repo, if active=true
    #[serde(skip_serializing_if = "Option::is_none")]
    pub rev: Option<Tid>,
    ///If active=false, this optional field indicates a possible reason for why the account is not active. If active=false and no status is supplied, then the host makes no claim for why the repository is no longer being hosted.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub status: Option<GetStatusOutputStatus<S>>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

/// If active=false, this optional field indicates a possible reason for why the account is not active. If active=false and no status is supplied, then the host makes no claim for why the repository is no longer being hosted.

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum GetStatusOutputStatus<S: BosStr = DefaultStr> {
    Takendown,
    Suspended,
    Deleted,
    Deactivated,
    Desynchronized,
    Throttled,
    Other(S),
}

impl<S: BosStr> GetStatusOutputStatus<S> {
    pub fn as_str(&self) -> &str {
        match self {
            Self::Takendown => "takendown",
            Self::Suspended => "suspended",
            Self::Deleted => "deleted",
            Self::Deactivated => "deactivated",
            Self::Desynchronized => "desynchronized",
            Self::Throttled => "throttled",
            Self::Other(s) => s.as_ref(),
        }
    }
    /// Construct from a string-like value, matching known values.
    pub fn from_value(s: S) -> Self {
        match s.as_ref() {
            "takendown" => Self::Takendown,
            "suspended" => Self::Suspended,
            "deleted" => Self::Deleted,
            "deactivated" => Self::Deactivated,
            "desynchronized" => Self::Desynchronized,
            "throttled" => Self::Throttled,
            _ => Self::Other(s),
        }
    }
}

impl<S: BosStr> core::fmt::Display for GetStatusOutputStatus<S> {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        write!(f, "{}", self.as_str())
    }
}

impl<S: BosStr> AsRef<str> for GetStatusOutputStatus<S> {
    fn as_ref(&self) -> &str {
        self.as_str()
    }
}

impl<S: BosStr> Serialize for GetStatusOutputStatus<S> {
    fn serialize<Ser>(&self, serializer: Ser) -> Result<Ser::Ok, Ser::Error>
    where
        Ser: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}

impl<'de, S: Deserialize<'de> + BosStr> Deserialize<'de> for GetStatusOutputStatus<S> {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = S::deserialize(deserializer)?;
        Ok(Self::from_value(s))
    }
}

impl<S: BosStr + Default> Default for GetStatusOutputStatus<S> {
    fn default() -> Self {
        Self::Other(Default::default())
    }
}

impl<S: BosStr> jacquard_common::IntoStatic for GetStatusOutputStatus<S>
where
    S: BosStr + jacquard_common::IntoStatic,
    S::Output: BosStr,
{
    type Output = GetStatusOutputStatus<S::Output>;
    fn into_static(self) -> Self::Output {
        match self {
            GetStatusOutputStatus::Takendown => GetStatusOutputStatus::Takendown,
            GetStatusOutputStatus::Suspended => GetStatusOutputStatus::Suspended,
            GetStatusOutputStatus::Deleted => GetStatusOutputStatus::Deleted,
            GetStatusOutputStatus::Deactivated => GetStatusOutputStatus::Deactivated,
            GetStatusOutputStatus::Desynchronized => {
                GetStatusOutputStatus::Desynchronized
            }
            GetStatusOutputStatus::Throttled => GetStatusOutputStatus::Throttled,
            GetStatusOutputStatus::Other(v) => {
                GetStatusOutputStatus::Other(v.into_static())
            }
        }
    }
}

/** Response marker for the `social.nstar.epds.repo.getStatus` query.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `GetStatusOutput<S>` for this endpoint.*/
pub struct GetStatusResponse;
impl jacquard_common::xrpc::XrpcResp for GetStatusResponse {
    const NSID: &'static str = "social.nstar.epds.repo.getStatus";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = GetStatusOutput<S>;
    type Err = jacquard_common::xrpc::GenericError;
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for GetStatus<S> {
    const NSID: &'static str = "social.nstar.epds.repo.getStatus";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Query;
    type Response = GetStatusResponse;
}

/** Endpoint marker for the `social.nstar.epds.repo.getStatus` query.

Path: `/xrpc/social.nstar.epds.repo.getStatus`. The request payload type is `GetStatus<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct GetStatusRequest;
impl jacquard_common::xrpc::XrpcEndpoint for GetStatusRequest {
    const PATH: &'static str = "/xrpc/social.nstar.epds.repo.getStatus";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Query;
    type Request<S: BosStr> = GetStatus<S>;
    type Response = GetStatusResponse;
}

pub mod get_status_state {

    pub use crate::builder_types::{Set, Unset, IsSet, IsUnset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Did;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Did = Unset;
    }
    ///State transition - sets the `did` field to Set
    pub struct SetDid<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetDid<St> {}
    impl<St: State> State for SetDid<St> {
        type Did = Set<members::did>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `did` field
        pub struct did(());
    }
}

/// Builder for constructing an instance of this type.
pub struct GetStatusBuilder<St: get_status_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<Did<S>>,),
    _type: PhantomData<fn() -> S>,
}

impl GetStatus<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> GetStatusBuilder<get_status_state::Empty, DefaultStr> {
        GetStatusBuilder::new()
    }
}

impl<S: BosStr> GetStatus<S> {
    /// Create a new builder for this type
    pub fn builder() -> GetStatusBuilder<get_status_state::Empty, S> {
        GetStatusBuilder::builder()
    }
}

impl GetStatusBuilder<get_status_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        GetStatusBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> GetStatusBuilder<get_status_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        GetStatusBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> GetStatusBuilder<St, S>
where
    St: get_status_state::State,
    St::Did: get_status_state::IsUnset,
{
    /// Set the `did` field (required)
    pub fn did(
        mut self,
        value: impl Into<Did<S>>,
    ) -> GetStatusBuilder<get_status_state::SetDid<St>, S> {
        self._fields.0 = Option::Some(value.into());
        GetStatusBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> GetStatusBuilder<St, S>
where
    St: get_status_state::State,
    St::Did: get_status_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> GetStatus<S> {
        GetStatus {
            did: self._fields.0.unwrap(),
        }
    }
}