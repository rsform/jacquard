// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: dev.atfs.repo.deleteFile
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::string::Cid;
use jacquard_common::types::value::Data;
use jacquard_common::{BosStr, CowStr, DefaultStr, FromStaticStr};
use jacquard_derive::{IntoStatic, open_union};
use serde::{Deserialize, Serialize};

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct DeleteFile<S: BosStr = DefaultStr> {
    /// The blessed CID (see dev.atfs.file) of the file to release the caller's claim on.
    pub cid: Cid<S>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct DeleteFileOutput<S: BosStr = DefaultStr> {
    /// Whether this call was the one that removed the last claim, triggering physical deletion of the stored bytes (and, for a large file, its UnixFS DAG artifacts). false means other accounts still hold claims on the cid, so it remains fully served.
    pub content_deleted: bool,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

#[derive(
    Serialize, Deserialize, Debug, Clone, PartialEq, Eq, thiserror::Error, miette::Diagnostic,
)]
#[serde(tag = "error", content = "message")]
pub enum DeleteFileError {
    /// No file is stored under this cid, and no pinFile request of the caller's is waiting on it either. Existence isn't secret — the /ipfs/<cid> gateway serves any stored cid to anyone — so this is reported distinctly from NotPinned rather than folded into it.
    #[serde(rename = "BlobNotFound")]
    BlobNotFound(#[serde(skip_serializing_if = "Option::is_none")] Option<SmolStr>),
    /// The file exists, but the caller holds no claim on it — there is nothing for this call to release.
    #[serde(rename = "NotPinned")]
    NotPinned(#[serde(skip_serializing_if = "Option::is_none")] Option<SmolStr>),
    /// Catch-all for unknown error codes.
    #[serde(untagged)]
    Other {
        error: SmolStr,
        #[serde(skip_serializing_if = "Option::is_none")]
        message: Option<SmolStr>,
    },
}

impl core::fmt::Display for DeleteFileError {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::BlobNotFound(msg) => {
                write!(f, "BlobNotFound")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::NotPinned(msg) => {
                write!(f, "NotPinned")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::Other { error, message } => {
                write!(f, "{}", error)?;
                if let Some(msg) = message {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
        }
    }
}

/** Response marker for the `dev.atfs.repo.deleteFile` procedure.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `DeleteFileOutput<S>` for this endpoint.*/
pub struct DeleteFileResponse;
impl jacquard_common::xrpc::XrpcResp for DeleteFileResponse {
    const NSID: &'static str = "dev.atfs.repo.deleteFile";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = DeleteFileOutput<S>;
    type Err = DeleteFileError;
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for DeleteFile<S> {
    const NSID: &'static str = "dev.atfs.repo.deleteFile";
    const METHOD: jacquard_common::xrpc::XrpcMethod =
        jacquard_common::xrpc::XrpcMethod::Procedure("application/json");
    type Response = DeleteFileResponse;
}

/** Endpoint marker for the `dev.atfs.repo.deleteFile` procedure.

Path: `/xrpc/dev.atfs.repo.deleteFile`. The request payload type is `DeleteFile<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct DeleteFileRequest;
impl jacquard_common::xrpc::XrpcEndpoint for DeleteFileRequest {
    const PATH: &'static str = "/xrpc/dev.atfs.repo.deleteFile";
    const METHOD: jacquard_common::xrpc::XrpcMethod =
        jacquard_common::xrpc::XrpcMethod::Procedure("application/json");
    type Request<S: BosStr> = DeleteFile<S>;
    type Response = DeleteFileResponse;
}

pub mod delete_file_state {

    pub use crate::builder_types::{IsSet, IsUnset, Set, Unset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Cid;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Cid = Unset;
    }
    ///State transition - sets the `cid` field to Set
    pub struct SetCid<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetCid<St> {}
    impl<St: State> State for SetCid<St> {
        type Cid = Set<members::cid>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `cid` field
        pub struct cid(());
    }
}

/// Builder for constructing an instance of this type.
pub struct DeleteFileBuilder<St: delete_file_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<Cid<S>>,),
    _type: PhantomData<fn() -> S>,
}

impl DeleteFile<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> DeleteFileBuilder<delete_file_state::Empty, DefaultStr> {
        DeleteFileBuilder::new()
    }
}

impl<S: BosStr> DeleteFile<S> {
    /// Create a new builder for this type
    pub fn builder() -> DeleteFileBuilder<delete_file_state::Empty, S> {
        DeleteFileBuilder::builder()
    }
}

impl DeleteFileBuilder<delete_file_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        DeleteFileBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> DeleteFileBuilder<delete_file_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        DeleteFileBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> DeleteFileBuilder<St, S>
where
    St: delete_file_state::State,
    St::Cid: delete_file_state::IsUnset,
{
    /// Set the `cid` field (required)
    pub fn cid(
        mut self,
        value: impl Into<Cid<S>>,
    ) -> DeleteFileBuilder<delete_file_state::SetCid<St>, S> {
        self._fields.0 = Option::Some(value.into());
        DeleteFileBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> DeleteFileBuilder<St, S>
where
    St: delete_file_state::State,
    St::Cid: delete_file_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> DeleteFile<S> {
        DeleteFile {
            cid: self._fields.0.unwrap(),
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(self, extra_data: BTreeMap<SmolStr, Data<S>>) -> DeleteFile<S> {
        DeleteFile {
            cid: self._fields.0.unwrap(),
            extra_data: Some(extra_data),
        }
    }
}
