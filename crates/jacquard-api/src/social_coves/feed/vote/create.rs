// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: social.coves.feed.vote.create
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

use crate::com_atproto::repo::strong_ref::StrongRef;
#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::string::{AtUri, Cid};
use jacquard_common::types::value::Data;
use jacquard_common::{BosStr, CowStr, DefaultStr, FromStaticStr};
use jacquard_derive::{IntoStatic, open_union};
use serde::{Deserialize, Serialize};

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct Create<S: BosStr = DefaultStr> {
    /// Vote direction: up for upvote, down for downvote
    pub direction: CreateDirection<S>,
    /// Strong reference to the post or comment being voted on
    pub subject: StrongRef<S>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

/// Vote direction: up for upvote, down for downvote

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum CreateDirection<S: BosStr = DefaultStr> {
    Up,
    Down,
    Other(S),
}

impl<S: BosStr> CreateDirection<S> {
    pub fn as_str(&self) -> &str {
        match self {
            Self::Up => "up",
            Self::Down => "down",
            Self::Other(s) => s.as_ref(),
        }
    }
    /// Construct from a string-like value, matching known values.
    pub fn from_value(s: S) -> Self {
        match s.as_ref() {
            "up" => Self::Up,
            "down" => Self::Down,
            _ => Self::Other(s),
        }
    }
}

impl<S: BosStr> core::fmt::Display for CreateDirection<S> {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        write!(f, "{}", self.as_str())
    }
}

impl<S: BosStr> AsRef<str> for CreateDirection<S> {
    fn as_ref(&self) -> &str {
        self.as_str()
    }
}

impl<S: BosStr> Serialize for CreateDirection<S> {
    fn serialize<Ser>(&self, serializer: Ser) -> Result<Ser::Ok, Ser::Error>
    where
        Ser: serde::Serializer,
    {
        serializer.serialize_str(self.as_str())
    }
}

impl<'de, S: Deserialize<'de> + BosStr> Deserialize<'de> for CreateDirection<S> {
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let s = S::deserialize(deserializer)?;
        Ok(Self::from_value(s))
    }
}

impl<S: BosStr + Default> Default for CreateDirection<S> {
    fn default() -> Self {
        Self::Other(Default::default())
    }
}

impl<S: BosStr> jacquard_common::IntoStatic for CreateDirection<S>
where
    S: BosStr + jacquard_common::IntoStatic,
    S::Output: BosStr,
{
    type Output = CreateDirection<S::Output>;
    fn into_static(self) -> Self::Output {
        match self {
            CreateDirection::Up => CreateDirection::Up,
            CreateDirection::Down => CreateDirection::Down,
            CreateDirection::Other(v) => CreateDirection::Other(v.into_static()),
        }
    }
}

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic, Default)]
#[serde(
    rename_all = "camelCase",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct CreateOutput<S: BosStr = DefaultStr> {
    /// CID of the created vote. Absent if the vote was toggled off.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub cid: Option<Cid<S>>,
    /// AT-URI of the created vote. Absent if the vote was toggled off.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub uri: Option<AtUri<S>>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

#[derive(
    Serialize, Deserialize, Debug, Clone, PartialEq, Eq, thiserror::Error, miette::Diagnostic,
)]
#[serde(tag = "error", content = "message")]
pub enum CreateError {
    /// User is not authorized to vote on this content
    #[serde(rename = "NotAuthorized")]
    NotAuthorized(#[serde(skip_serializing_if = "Option::is_none")] Option<SmolStr>),
    /// The subject reference is invalid or malformed
    #[serde(rename = "InvalidSubject")]
    InvalidSubject(#[serde(skip_serializing_if = "Option::is_none")] Option<SmolStr>),
    /// Catch-all for unknown error codes.
    #[serde(untagged)]
    Other {
        error: SmolStr,
        #[serde(skip_serializing_if = "Option::is_none")]
        message: Option<SmolStr>,
    },
}

impl core::fmt::Display for CreateError {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::NotAuthorized(msg) => {
                write!(f, "NotAuthorized")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::InvalidSubject(msg) => {
                write!(f, "InvalidSubject")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::Other { error, message } => {
                write!(f, "{}", error)?;
                if let Some(msg) = message {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
        }
    }
}

/** Response marker for the `social.coves.feed.vote.create` procedure.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `CreateOutput<S>` for this endpoint.*/
pub struct CreateResponse;
impl jacquard_common::xrpc::XrpcResp for CreateResponse {
    const NSID: &'static str = "social.coves.feed.vote.create";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = CreateOutput<S>;
    type Err = CreateError;
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for Create<S> {
    const NSID: &'static str = "social.coves.feed.vote.create";
    const METHOD: jacquard_common::xrpc::XrpcMethod =
        jacquard_common::xrpc::XrpcMethod::Procedure("application/json");
    type Response = CreateResponse;
}

/** Endpoint marker for the `social.coves.feed.vote.create` procedure.

Path: `/xrpc/social.coves.feed.vote.create`. The request payload type is `Create<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct CreateRequest;
impl jacquard_common::xrpc::XrpcEndpoint for CreateRequest {
    const PATH: &'static str = "/xrpc/social.coves.feed.vote.create";
    const METHOD: jacquard_common::xrpc::XrpcMethod =
        jacquard_common::xrpc::XrpcMethod::Procedure("application/json");
    type Request<S: BosStr> = Create<S>;
    type Response = CreateResponse;
}

pub mod create_state {

    pub use crate::builder_types::{IsSet, IsUnset, Set, Unset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Direction;
        type Subject;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Direction = Unset;
        type Subject = Unset;
    }
    ///State transition - sets the `direction` field to Set
    pub struct SetDirection<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetDirection<St> {}
    impl<St: State> State for SetDirection<St> {
        type Direction = Set<members::direction>;
        type Subject = St::Subject;
    }
    ///State transition - sets the `subject` field to Set
    pub struct SetSubject<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetSubject<St> {}
    impl<St: State> State for SetSubject<St> {
        type Direction = St::Direction;
        type Subject = Set<members::subject>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `direction` field
        pub struct direction(());
        ///Marker type for the `subject` field
        pub struct subject(());
    }
}

/// Builder for constructing an instance of this type.
pub struct CreateBuilder<St: create_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<CreateDirection<S>>, Option<StrongRef<S>>),
    _type: PhantomData<fn() -> S>,
}

impl Create<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> CreateBuilder<create_state::Empty, DefaultStr> {
        CreateBuilder::new()
    }
}

impl<S: BosStr> Create<S> {
    /// Create a new builder for this type
    pub fn builder() -> CreateBuilder<create_state::Empty, S> {
        CreateBuilder::builder()
    }
}

impl CreateBuilder<create_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        CreateBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> CreateBuilder<create_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        CreateBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> CreateBuilder<St, S>
where
    St: create_state::State,
    St::Direction: create_state::IsUnset,
{
    /// Set the `direction` field (required)
    pub fn direction(
        mut self,
        value: impl Into<CreateDirection<S>>,
    ) -> CreateBuilder<create_state::SetDirection<St>, S> {
        self._fields.0 = Option::Some(value.into());
        CreateBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> CreateBuilder<St, S>
where
    St: create_state::State,
    St::Subject: create_state::IsUnset,
{
    /// Set the `subject` field (required)
    pub fn subject(
        mut self,
        value: impl Into<StrongRef<S>>,
    ) -> CreateBuilder<create_state::SetSubject<St>, S> {
        self._fields.1 = Option::Some(value.into());
        CreateBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> CreateBuilder<St, S>
where
    St: create_state::State,
    St::Direction: create_state::IsSet,
    St::Subject: create_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> Create<S> {
        Create {
            direction: self._fields.0.unwrap(),
            subject: self._fields.1.unwrap(),
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(self, extra_data: BTreeMap<SmolStr, Data<S>>) -> Create<S> {
        Create {
            direction: self._fields.0.unwrap(),
            subject: self._fields.1.unwrap(),
            extra_data: Some(extra_data),
        }
    }
}
