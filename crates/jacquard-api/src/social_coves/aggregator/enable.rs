// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: social.coves.aggregator.enable
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{CowStr, BosStr, DefaultStr, FromStaticStr};
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::ident::AtIdentifier;
use jacquard_common::types::string::{Did, AtUri, Cid};
use jacquard_common::types::value::Data;
use jacquard_derive::{IntoStatic, open_union};
use serde::{Serialize, Deserialize};
use crate::social_coves::aggregator::AuthorizationView;

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct Enable<S: BosStr = DefaultStr> {
    ///DID of the aggregator to enable
    pub aggregator_did: Did<S>,
    ///DID or handle of the community
    pub community: AtIdentifier<S>,
    ///Aggregator-specific configuration. Must conform to the aggregator's configSchema.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub config: Option<Data<S>>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}


#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct EnableOutput<S: BosStr = DefaultStr> {
    ///The created authorization details
    pub authorization: AuthorizationView<S>,
    ///CID of the created authorization record
    pub cid: Cid<S>,
    ///AT-URI of the created authorization record
    pub uri: AtUri<S>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}


#[derive(
    Serialize,
    Deserialize,
    Debug,
    Clone,
    PartialEq,
    Eq,
    thiserror::Error,
    miette::Diagnostic
)]

#[serde(tag = "error", content = "message")]
pub enum EnableError {
    /// Caller is not a moderator of this community
    #[serde(rename = "NotAuthorized")]
    NotAuthorized(Option<SmolStr>),
    /// Aggregator DID does not exist or has no service declaration
    #[serde(rename = "AggregatorNotFound")]
    AggregatorNotFound(Option<SmolStr>),
    /// Config does not match aggregator's configSchema
    #[serde(rename = "InvalidConfig")]
    InvalidConfig(Option<SmolStr>),
    /// Aggregator is already enabled for this community
    #[serde(rename = "AlreadyEnabled")]
    AlreadyEnabled(Option<SmolStr>),
    /// Catch-all for unknown error codes.
    #[serde(untagged)]
    Other { error: SmolStr, message: Option<SmolStr> },
}

impl core::fmt::Display for EnableError {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::NotAuthorized(msg) => {
                write!(f, "NotAuthorized")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::AggregatorNotFound(msg) => {
                write!(f, "AggregatorNotFound")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::InvalidConfig(msg) => {
                write!(f, "InvalidConfig")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::AlreadyEnabled(msg) => {
                write!(f, "AlreadyEnabled")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::Other { error, message } => {
                write!(f, "{}", error)?;
                if let Some(msg) = message {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
        }
    }
}

/** Response marker for the `social.coves.aggregator.enable` procedure.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `EnableOutput<S>` for this endpoint.*/
pub struct EnableResponse;
impl jacquard_common::xrpc::XrpcResp for EnableResponse {
    const NSID: &'static str = "social.coves.aggregator.enable";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = EnableOutput<S>;
    type Err = EnableError;
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for Enable<S> {
    const NSID: &'static str = "social.coves.aggregator.enable";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Procedure(
        "application/json",
    );
    type Response = EnableResponse;
}

/** Endpoint marker for the `social.coves.aggregator.enable` procedure.

Path: `/xrpc/social.coves.aggregator.enable`. The request payload type is `Enable<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct EnableRequest;
impl jacquard_common::xrpc::XrpcEndpoint for EnableRequest {
    const PATH: &'static str = "/xrpc/social.coves.aggregator.enable";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Procedure(
        "application/json",
    );
    type Request<S: BosStr> = Enable<S>;
    type Response = EnableResponse;
}

pub mod enable_state {

    pub use crate::builder_types::{Set, Unset, IsSet, IsUnset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type AggregatorDid;
        type Community;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type AggregatorDid = Unset;
        type Community = Unset;
    }
    ///State transition - sets the `aggregator_did` field to Set
    pub struct SetAggregatorDid<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetAggregatorDid<St> {}
    impl<St: State> State for SetAggregatorDid<St> {
        type AggregatorDid = Set<members::aggregator_did>;
        type Community = St::Community;
    }
    ///State transition - sets the `community` field to Set
    pub struct SetCommunity<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetCommunity<St> {}
    impl<St: State> State for SetCommunity<St> {
        type AggregatorDid = St::AggregatorDid;
        type Community = Set<members::community>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `aggregator_did` field
        pub struct aggregator_did(());
        ///Marker type for the `community` field
        pub struct community(());
    }
}

/// Builder for constructing an instance of this type.
pub struct EnableBuilder<St: enable_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<Did<S>>, Option<AtIdentifier<S>>, Option<Data<S>>),
    _type: PhantomData<fn() -> S>,
}

impl Enable<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> EnableBuilder<enable_state::Empty, DefaultStr> {
        EnableBuilder::new()
    }
}

impl<S: BosStr> Enable<S> {
    /// Create a new builder for this type
    pub fn builder() -> EnableBuilder<enable_state::Empty, S> {
        EnableBuilder::builder()
    }
}

impl EnableBuilder<enable_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        EnableBuilder {
            _state: PhantomData,
            _fields: (None, None, None),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> EnableBuilder<enable_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        EnableBuilder {
            _state: PhantomData,
            _fields: (None, None, None),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> EnableBuilder<St, S>
where
    St: enable_state::State,
    St::AggregatorDid: enable_state::IsUnset,
{
    /// Set the `aggregatorDid` field (required)
    pub fn aggregator_did(
        mut self,
        value: impl Into<Did<S>>,
    ) -> EnableBuilder<enable_state::SetAggregatorDid<St>, S> {
        self._fields.0 = Option::Some(value.into());
        EnableBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> EnableBuilder<St, S>
where
    St: enable_state::State,
    St::Community: enable_state::IsUnset,
{
    /// Set the `community` field (required)
    pub fn community(
        mut self,
        value: impl Into<AtIdentifier<S>>,
    ) -> EnableBuilder<enable_state::SetCommunity<St>, S> {
        self._fields.1 = Option::Some(value.into());
        EnableBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St: enable_state::State, S: BosStr> EnableBuilder<St, S> {
    /// Set the `config` field (optional)
    pub fn config(mut self, value: impl Into<Option<Data<S>>>) -> Self {
        self._fields.2 = value.into();
        self
    }
    /// Set the `config` field to an Option value (optional)
    pub fn maybe_config(mut self, value: Option<Data<S>>) -> Self {
        self._fields.2 = value;
        self
    }
}

impl<St, S: BosStr> EnableBuilder<St, S>
where
    St: enable_state::State,
    St::AggregatorDid: enable_state::IsSet,
    St::Community: enable_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> Enable<S> {
        Enable {
            aggregator_did: self._fields.0.unwrap(),
            community: self._fields.1.unwrap(),
            config: self._fields.2,
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(self, extra_data: BTreeMap<SmolStr, Data<S>>) -> Enable<S> {
        Enable {
            aggregator_did: self._fields.0.unwrap(),
            community: self._fields.1.unwrap(),
            config: self._fields.2,
            extra_data: Some(extra_data),
        }
    }
}