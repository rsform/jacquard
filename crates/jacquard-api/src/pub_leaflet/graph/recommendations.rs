// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: pub.leaflet.graph.recommendations
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{BosStr, CowStr, DefaultStr, FromStaticStr};

#[allow(unused_imports)]
use jacquard_common::deps::codegen::unicode_segmentation::UnicodeSegmentation;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::collection::{Collection, RecordError};
use jacquard_common::types::string::{AtUri, Cid};
use jacquard_common::types::uri::{RecordUri, UriError};
use jacquard_common::types::value::Data;
use jacquard_common::xrpc::XrpcResp;
use jacquard_derive::{IntoStatic, lexicon};
use jacquard_lexicon::lexicon::LexiconDoc;
use jacquard_lexicon::schema::LexiconSchema;

#[allow(unused_imports)]
use jacquard_lexicon::validation::{ConstraintError, ValidationPath};
use serde::{Deserialize, Serialize};
/// Record declaring the publications a publication recommends

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    rename = "pub.leaflet.graph.recommendations",
    tag = "$type",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct Recommendations<S: BosStr = DefaultStr> {
    ///The publication making the recommendations; must live in the same repo as this record
    pub publication: AtUri<S>,
    ///Publications this publication recommends
    pub recommendations: Vec<AtUri<S>>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

/// Typed wrapper for GetRecord response with this collection's record type.

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase")]
pub struct RecommendationsGetRecordOutput<S: BosStr = DefaultStr> {
    #[serde(skip_serializing_if = "Option::is_none")]
    pub cid: Option<Cid<S>>,
    pub uri: AtUri<S>,
    pub value: Recommendations<S>,
}

impl<S: BosStr> Recommendations<S> {
    pub fn uri(uri: S) -> Result<RecordUri<S, RecommendationsRecord>, UriError> {
        RecordUri::try_from_uri(AtUri::new(uri)?)
    }
}

/// Marker type for deserializing records from this collection.

#[derive(Debug, Serialize, Deserialize)]
pub struct RecommendationsRecord;
impl XrpcResp for RecommendationsRecord {
    const NSID: &'static str = "pub.leaflet.graph.recommendations";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = RecommendationsGetRecordOutput<S>;
    type Err = RecordError;
}

impl<S: BosStr> From<RecommendationsGetRecordOutput<S>> for Recommendations<S> {
    fn from(output: RecommendationsGetRecordOutput<S>) -> Self {
        output.value
    }
}

impl<S: BosStr> Collection for Recommendations<S> {
    const NSID: &'static str = "pub.leaflet.graph.recommendations";
    type Record = RecommendationsRecord;
}

impl Collection for RecommendationsRecord {
    const NSID: &'static str = "pub.leaflet.graph.recommendations";
    type Record = RecommendationsRecord;
}

impl<S: BosStr> LexiconSchema for Recommendations<S> {
    fn nsid() -> &'static str {
        "pub.leaflet.graph.recommendations"
    }
    fn def_name() -> &'static str {
        "main"
    }
    fn lexicon_doc() -> LexiconDoc<'static> {
        lexicon_doc_pub_leaflet_graph_recommendations()
    }
    fn validate(&self) -> Result<(), ConstraintError> {
        {
            let value = &self.recommendations;
            #[allow(unused_comparisons)]
            if value.len() > 3usize {
                return Err(ConstraintError::MaxLength {
                    path: ValidationPath::from_field("recommendations"),
                    max: 3usize,
                    actual: value.len(),
                });
            }
        }
        Ok(())
    }
}

pub mod recommendations_state {

    pub use crate::builder_types::{IsSet, IsUnset, Set, Unset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Publication;
        type Recommendations;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Publication = Unset;
        type Recommendations = Unset;
    }
    ///State transition - sets the `publication` field to Set
    pub struct SetPublication<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetPublication<St> {}
    impl<St: State> State for SetPublication<St> {
        type Publication = Set<members::publication>;
        type Recommendations = St::Recommendations;
    }
    ///State transition - sets the `recommendations` field to Set
    pub struct SetRecommendations<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetRecommendations<St> {}
    impl<St: State> State for SetRecommendations<St> {
        type Publication = St::Publication;
        type Recommendations = Set<members::recommendations>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `publication` field
        pub struct publication(());
        ///Marker type for the `recommendations` field
        pub struct recommendations(());
    }
}

/// Builder for constructing an instance of this type.
pub struct RecommendationsBuilder<St: recommendations_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<AtUri<S>>, Option<Vec<AtUri<S>>>),
    _type: PhantomData<fn() -> S>,
}

impl Recommendations<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> RecommendationsBuilder<recommendations_state::Empty, DefaultStr> {
        RecommendationsBuilder::new()
    }
}

impl<S: BosStr> Recommendations<S> {
    /// Create a new builder for this type
    pub fn builder() -> RecommendationsBuilder<recommendations_state::Empty, S> {
        RecommendationsBuilder::builder()
    }
}

impl RecommendationsBuilder<recommendations_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        RecommendationsBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> RecommendationsBuilder<recommendations_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        RecommendationsBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> RecommendationsBuilder<St, S>
where
    St: recommendations_state::State,
    St::Publication: recommendations_state::IsUnset,
{
    /// Set the `publication` field (required)
    pub fn publication(
        mut self,
        value: impl Into<AtUri<S>>,
    ) -> RecommendationsBuilder<recommendations_state::SetPublication<St>, S> {
        self._fields.0 = Option::Some(value.into());
        RecommendationsBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> RecommendationsBuilder<St, S>
where
    St: recommendations_state::State,
    St::Recommendations: recommendations_state::IsUnset,
{
    /// Set the `recommendations` field (required)
    pub fn recommendations(
        mut self,
        value: impl Into<Vec<AtUri<S>>>,
    ) -> RecommendationsBuilder<recommendations_state::SetRecommendations<St>, S> {
        self._fields.1 = Option::Some(value.into());
        RecommendationsBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> RecommendationsBuilder<St, S>
where
    St: recommendations_state::State,
    St::Publication: recommendations_state::IsSet,
    St::Recommendations: recommendations_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> Recommendations<S> {
        Recommendations {
            publication: self._fields.0.unwrap(),
            recommendations: self._fields.1.unwrap(),
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(self, extra_data: BTreeMap<SmolStr, Data<S>>) -> Recommendations<S> {
        Recommendations {
            publication: self._fields.0.unwrap(),
            recommendations: self._fields.1.unwrap(),
            extra_data: Some(extra_data),
        }
    }
}

fn lexicon_doc_pub_leaflet_graph_recommendations() -> LexiconDoc<'static> {
    use alloc::collections::BTreeMap;
    #[allow(unused_imports)]
    use jacquard_common::{CowStr, deps::smol_str::SmolStr, types::blob::MimeType};
    use jacquard_lexicon::lexicon::*;
    LexiconDoc {
        lexicon: Lexicon::Lexicon1,
        id: CowStr::new_static("pub.leaflet.graph.recommendations"),
        defs: {
            let mut map = BTreeMap::new();
            map.insert(
                SmolStr::new_static("main"),
                LexUserType::Record(LexRecord {
                    description: Some(
                        CowStr::new_static(
                            "Record declaring the publications a publication recommends",
                        ),
                    ),
                    key: Some(CowStr::new_static("any")),
                    record: LexRecordRecord::Object(LexObject {
                        required: Some(
                            vec![
                                SmolStr::new_static("publication"),
                                SmolStr::new_static("recommendations")
                            ],
                        ),
                        properties: {
                            #[allow(unused_mut)]
                            let mut map = BTreeMap::new();
                            map.insert(
                                SmolStr::new_static("publication"),
                                LexObjectProperty::String(LexString {
                                    description: Some(
                                        CowStr::new_static(
                                            "The publication making the recommendations; must live in the same repo as this record",
                                        ),
                                    ),
                                    format: Some(LexStringFormat::AtUri),
                                    ..Default::default()
                                }),
                            );
                            map.insert(
                                SmolStr::new_static("recommendations"),
                                LexObjectProperty::Array(LexArray {
                                    description: Some(
                                        CowStr::new_static(
                                            "Publications this publication recommends",
                                        ),
                                    ),
                                    items: LexArrayItem::String(LexString {
                                        format: Some(LexStringFormat::AtUri),
                                        ..Default::default()
                                    }),
                                    max_length: Some(3usize),
                                    ..Default::default()
                                }),
                            );
                            map
                        },
                        ..Default::default()
                    }),
                    ..Default::default()
                }),
            );
            map
        },
        ..Default::default()
    }
}
