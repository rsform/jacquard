// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: net.anisota.graph.scopedMute
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{BosStr, CowStr, DefaultStr, FromStaticStr};

#[allow(unused_imports)]
use jacquard_common::deps::codegen::unicode_segmentation::UnicodeSegmentation;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::collection::{Collection, RecordError};
use jacquard_common::types::string::{AtUri, Cid, Datetime, Did};
use jacquard_common::types::uri::{RecordUri, UriError};
use jacquard_common::types::value::Data;
use jacquard_common::xrpc::XrpcResp;
use jacquard_derive::{IntoStatic, lexicon};
use jacquard_lexicon::lexicon::LexiconDoc;
use jacquard_lexicon::schema::LexiconSchema;

use crate::net_anisota::graph::scoped_mute;
#[allow(unused_imports)]
use jacquard_lexicon::validation::{ConstraintError, ValidationPath};
use serde::{Deserialize, Serialize};
/// An index of the accounts whose reposts or quote posts the viewer has muted through Bluesky's native scoped mute (app.bsky.graph.muteActor with onlyReposts / onlyQuoteposts). Bluesky stores the mute itself, but app.bsky.graph.getMutes returns only fully muted accounts, so a scoped mute is otherwise discoverable only by loading each profile. This record lets Anisota list and undo them, and enforce them client-side on unauthenticated reads where no viewer state comes back. It is a cache, not the source of truth: entries are reconciled against viewerState.mutedOnlyReposts / mutedOnlyQuoteposts and dropped when the server disagrees.

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    rename = "net.anisota.graph.scopedMute",
    tag = "$type",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct ScopedMute<S: BosStr = DefaultStr> {
    ///When this index was first written
    pub created_at: Datetime,
    ///The accounts currently carrying a native scoped mute.
    pub subjects: Vec<scoped_mute::ScopedMuteEntry<S>>,
    ///When this index was last reconciled or edited
    #[serde(skip_serializing_if = "Option::is_none")]
    pub updated_at: Option<Datetime>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

/// Typed wrapper for GetRecord response with this collection's record type.

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase")]
pub struct ScopedMuteGetRecordOutput<S: BosStr = DefaultStr> {
    #[serde(skip_serializing_if = "Option::is_none")]
    pub cid: Option<Cid<S>>,
    pub uri: AtUri<S>,
    pub value: ScopedMute<S>,
}

/// One account's native scoped mute. At least one scope flag is true — an entry with neither is a full mute or no mute at all, and does not belong here.

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct ScopedMuteEntry<S: BosStr = DefaultStr> {
    ///When this scoped mute was set from Anisota
    #[serde(skip_serializing_if = "Option::is_none")]
    pub created_at: Option<Datetime>,
    ///Their quote posts are muted  Defaults to `false`.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(default = "_default_scoped_mute_entry_only_quoteposts")]
    pub only_quoteposts: Option<bool>,
    ///Their reposts are muted  Defaults to `false`.
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(default = "_default_scoped_mute_entry_only_reposts")]
    pub only_reposts: Option<bool>,
    ///DID of the scope-muted account
    pub subject: Did<S>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

impl<S: BosStr> ScopedMute<S> {
    pub fn uri(uri: S) -> Result<RecordUri<S, ScopedMuteRecord>, UriError> {
        RecordUri::try_from_uri(AtUri::new(uri)?)
    }
}

/// Marker type for deserializing records from this collection.

#[derive(Debug, Serialize, Deserialize)]
pub struct ScopedMuteRecord;
impl XrpcResp for ScopedMuteRecord {
    const NSID: &'static str = "net.anisota.graph.scopedMute";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = ScopedMuteGetRecordOutput<S>;
    type Err = RecordError;
}

impl<S: BosStr> From<ScopedMuteGetRecordOutput<S>> for ScopedMute<S> {
    fn from(output: ScopedMuteGetRecordOutput<S>) -> Self {
        output.value
    }
}

impl<S: BosStr> Collection for ScopedMute<S> {
    const NSID: &'static str = "net.anisota.graph.scopedMute";
    type Record = ScopedMuteRecord;
}

impl Collection for ScopedMuteRecord {
    const NSID: &'static str = "net.anisota.graph.scopedMute";
    type Record = ScopedMuteRecord;
}

impl<S: BosStr> LexiconSchema for ScopedMute<S> {
    fn nsid() -> &'static str {
        "net.anisota.graph.scopedMute"
    }
    fn def_name() -> &'static str {
        "main"
    }
    fn lexicon_doc() -> LexiconDoc<'static> {
        lexicon_doc_net_anisota_graph_scopedMute()
    }
    fn validate(&self) -> Result<(), ConstraintError> {
        {
            let value = &self.subjects;
            #[allow(unused_comparisons)]
            if value.len() > 1000usize {
                return Err(ConstraintError::MaxLength {
                    path: ValidationPath::from_field("subjects"),
                    max: 1000usize,
                    actual: value.len(),
                });
            }
        }
        Ok(())
    }
}

impl<S: BosStr> LexiconSchema for ScopedMuteEntry<S> {
    fn nsid() -> &'static str {
        "net.anisota.graph.scopedMute"
    }
    fn def_name() -> &'static str {
        "scopedMuteEntry"
    }
    fn lexicon_doc() -> LexiconDoc<'static> {
        lexicon_doc_net_anisota_graph_scopedMute()
    }
    fn validate(&self) -> Result<(), ConstraintError> {
        Ok(())
    }
}

pub mod scoped_mute_state {

    pub use crate::builder_types::{IsSet, IsUnset, Set, Unset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type CreatedAt;
        type Subjects;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type CreatedAt = Unset;
        type Subjects = Unset;
    }
    ///State transition - sets the `created_at` field to Set
    pub struct SetCreatedAt<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetCreatedAt<St> {}
    impl<St: State> State for SetCreatedAt<St> {
        type CreatedAt = Set<members::created_at>;
        type Subjects = St::Subjects;
    }
    ///State transition - sets the `subjects` field to Set
    pub struct SetSubjects<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetSubjects<St> {}
    impl<St: State> State for SetSubjects<St> {
        type CreatedAt = St::CreatedAt;
        type Subjects = Set<members::subjects>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `created_at` field
        pub struct created_at(());
        ///Marker type for the `subjects` field
        pub struct subjects(());
    }
}

/// Builder for constructing an instance of this type.
pub struct ScopedMuteBuilder<St: scoped_mute_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (
        Option<Datetime>,
        Option<Vec<scoped_mute::ScopedMuteEntry<S>>>,
        Option<Datetime>,
    ),
    _type: PhantomData<fn() -> S>,
}

impl ScopedMute<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> ScopedMuteBuilder<scoped_mute_state::Empty, DefaultStr> {
        ScopedMuteBuilder::new()
    }
}

impl<S: BosStr> ScopedMute<S> {
    /// Create a new builder for this type
    pub fn builder() -> ScopedMuteBuilder<scoped_mute_state::Empty, S> {
        ScopedMuteBuilder::builder()
    }
}

impl ScopedMuteBuilder<scoped_mute_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        ScopedMuteBuilder {
            _state: PhantomData,
            _fields: (None, None, None),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> ScopedMuteBuilder<scoped_mute_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        ScopedMuteBuilder {
            _state: PhantomData,
            _fields: (None, None, None),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> ScopedMuteBuilder<St, S>
where
    St: scoped_mute_state::State,
    St::CreatedAt: scoped_mute_state::IsUnset,
{
    /// Set the `createdAt` field (required)
    pub fn created_at(
        mut self,
        value: impl Into<Datetime>,
    ) -> ScopedMuteBuilder<scoped_mute_state::SetCreatedAt<St>, S> {
        self._fields.0 = Option::Some(value.into());
        ScopedMuteBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> ScopedMuteBuilder<St, S>
where
    St: scoped_mute_state::State,
    St::Subjects: scoped_mute_state::IsUnset,
{
    /// Set the `subjects` field (required)
    pub fn subjects(
        mut self,
        value: impl Into<Vec<scoped_mute::ScopedMuteEntry<S>>>,
    ) -> ScopedMuteBuilder<scoped_mute_state::SetSubjects<St>, S> {
        self._fields.1 = Option::Some(value.into());
        ScopedMuteBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St: scoped_mute_state::State, S: BosStr> ScopedMuteBuilder<St, S> {
    /// Set the `updatedAt` field (optional)
    pub fn updated_at(mut self, value: impl Into<Option<Datetime>>) -> Self {
        self._fields.2 = value.into();
        self
    }
    /// Set the `updatedAt` field to an Option value (optional)
    pub fn maybe_updated_at(mut self, value: Option<Datetime>) -> Self {
        self._fields.2 = value;
        self
    }
}

impl<St, S: BosStr> ScopedMuteBuilder<St, S>
where
    St: scoped_mute_state::State,
    St::CreatedAt: scoped_mute_state::IsSet,
    St::Subjects: scoped_mute_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> ScopedMute<S> {
        ScopedMute {
            created_at: self._fields.0.unwrap(),
            subjects: self._fields.1.unwrap(),
            updated_at: self._fields.2,
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(self, extra_data: BTreeMap<SmolStr, Data<S>>) -> ScopedMute<S> {
        ScopedMute {
            created_at: self._fields.0.unwrap(),
            subjects: self._fields.1.unwrap(),
            updated_at: self._fields.2,
            extra_data: Some(extra_data),
        }
    }
}

fn lexicon_doc_net_anisota_graph_scopedMute() -> LexiconDoc<'static> {
    use alloc::collections::BTreeMap;
    #[allow(unused_imports)]
    use jacquard_common::{CowStr, deps::smol_str::SmolStr, types::blob::MimeType};
    use jacquard_lexicon::lexicon::*;
    LexiconDoc {
        lexicon: Lexicon::Lexicon1,
        id: CowStr::new_static("net.anisota.graph.scopedMute"),
        defs: {
            let mut map = BTreeMap::new();
            map.insert(
                SmolStr::new_static("main"),
                LexUserType::Record(LexRecord {
                    description: Some(
                        CowStr::new_static(
                            "An index of the accounts whose reposts or quote posts the viewer has muted through Bluesky's native scoped mute (app.bsky.graph.muteActor with onlyReposts / onlyQuoteposts). Bluesky stores the mute itself, but app.bsky.graph.getMutes returns only fully muted accounts, so a scoped mute is otherwise discoverable only by loading each profile. This record lets Anisota list and undo them, and enforce them client-side on unauthenticated reads where no viewer state comes back. It is a cache, not the source of truth: entries are reconciled against viewerState.mutedOnlyReposts / mutedOnlyQuoteposts and dropped when the server disagrees.",
                        ),
                    ),
                    key: Some(CowStr::new_static("literal:self")),
                    record: LexRecordRecord::Object(LexObject {
                        required: Some(
                            vec![
                                SmolStr::new_static("subjects"),
                                SmolStr::new_static("createdAt")
                            ],
                        ),
                        properties: {
                            #[allow(unused_mut)]
                            let mut map = BTreeMap::new();
                            map.insert(
                                SmolStr::new_static("createdAt"),
                                LexObjectProperty::String(LexString {
                                    description: Some(
                                        CowStr::new_static("When this index was first written"),
                                    ),
                                    format: Some(LexStringFormat::Datetime),
                                    ..Default::default()
                                }),
                            );
                            map.insert(
                                SmolStr::new_static("subjects"),
                                LexObjectProperty::Array(LexArray {
                                    description: Some(
                                        CowStr::new_static(
                                            "The accounts currently carrying a native scoped mute.",
                                        ),
                                    ),
                                    items: LexArrayItem::Ref(LexRef {
                                        r#ref: CowStr::new_static("#scopedMuteEntry"),
                                        ..Default::default()
                                    }),
                                    max_length: Some(1000usize),
                                    ..Default::default()
                                }),
                            );
                            map.insert(
                                SmolStr::new_static("updatedAt"),
                                LexObjectProperty::String(LexString {
                                    description: Some(
                                        CowStr::new_static(
                                            "When this index was last reconciled or edited",
                                        ),
                                    ),
                                    format: Some(LexStringFormat::Datetime),
                                    ..Default::default()
                                }),
                            );
                            map
                        },
                        ..Default::default()
                    }),
                    ..Default::default()
                }),
            );
            map.insert(
                SmolStr::new_static("scopedMuteEntry"),
                LexUserType::Object(LexObject {
                    description: Some(
                        CowStr::new_static(
                            "One account's native scoped mute. At least one scope flag is true — an entry with neither is a full mute or no mute at all, and does not belong here.",
                        ),
                    ),
                    required: Some(vec![SmolStr::new_static("subject")]),
                    properties: {
                        #[allow(unused_mut)]
                        let mut map = BTreeMap::new();
                        map.insert(
                            SmolStr::new_static("createdAt"),
                            LexObjectProperty::String(LexString {
                                description: Some(
                                    CowStr::new_static(
                                        "When this scoped mute was set from Anisota",
                                    ),
                                ),
                                format: Some(LexStringFormat::Datetime),
                                ..Default::default()
                            }),
                        );
                        map.insert(
                            SmolStr::new_static("onlyQuoteposts"),
                            LexObjectProperty::Boolean(LexBoolean {
                                ..Default::default()
                            }),
                        );
                        map.insert(
                            SmolStr::new_static("onlyReposts"),
                            LexObjectProperty::Boolean(LexBoolean {
                                ..Default::default()
                            }),
                        );
                        map.insert(
                            SmolStr::new_static("subject"),
                            LexObjectProperty::String(LexString {
                                description: Some(
                                    CowStr::new_static("DID of the scope-muted account"),
                                ),
                                format: Some(LexStringFormat::Did),
                                ..Default::default()
                            }),
                        );
                        map
                    },
                    ..Default::default()
                }),
            );
            map
        },
        ..Default::default()
    }
}

fn _default_scoped_mute_entry_only_quoteposts() -> Option<bool> {
    Some(false)
}

fn _default_scoped_mute_entry_only_reposts() -> Option<bool> {
    Some(false)
}

pub mod scoped_mute_entry_state {

    pub use crate::builder_types::{IsSet, IsUnset, Set, Unset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Subject;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Subject = Unset;
    }
    ///State transition - sets the `subject` field to Set
    pub struct SetSubject<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetSubject<St> {}
    impl<St: State> State for SetSubject<St> {
        type Subject = Set<members::subject>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `subject` field
        pub struct subject(());
    }
}

/// Builder for constructing an instance of this type.
pub struct ScopedMuteEntryBuilder<St: scoped_mute_entry_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<Datetime>, Option<bool>, Option<bool>, Option<Did<S>>),
    _type: PhantomData<fn() -> S>,
}

impl ScopedMuteEntry<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> ScopedMuteEntryBuilder<scoped_mute_entry_state::Empty, DefaultStr> {
        ScopedMuteEntryBuilder::new()
    }
}

impl<S: BosStr> ScopedMuteEntry<S> {
    /// Create a new builder for this type
    pub fn builder() -> ScopedMuteEntryBuilder<scoped_mute_entry_state::Empty, S> {
        ScopedMuteEntryBuilder::builder()
    }
}

impl ScopedMuteEntryBuilder<scoped_mute_entry_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        ScopedMuteEntryBuilder {
            _state: PhantomData,
            _fields: (None, None, None, None),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> ScopedMuteEntryBuilder<scoped_mute_entry_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        ScopedMuteEntryBuilder {
            _state: PhantomData,
            _fields: (None, None, None, None),
            _type: PhantomData,
        }
    }
}

impl<St: scoped_mute_entry_state::State, S: BosStr> ScopedMuteEntryBuilder<St, S> {
    /// Set the `createdAt` field (optional)
    pub fn created_at(mut self, value: impl Into<Option<Datetime>>) -> Self {
        self._fields.0 = value.into();
        self
    }
    /// Set the `createdAt` field to an Option value (optional)
    pub fn maybe_created_at(mut self, value: Option<Datetime>) -> Self {
        self._fields.0 = value;
        self
    }
}

impl<St: scoped_mute_entry_state::State, S: BosStr> ScopedMuteEntryBuilder<St, S> {
    /// Set the `onlyQuoteposts` field (optional)
    pub fn only_quoteposts(mut self, value: impl Into<Option<bool>>) -> Self {
        self._fields.1 = value.into();
        self
    }
    /// Set the `onlyQuoteposts` field to an Option value (optional)
    pub fn maybe_only_quoteposts(mut self, value: Option<bool>) -> Self {
        self._fields.1 = value;
        self
    }
}

impl<St: scoped_mute_entry_state::State, S: BosStr> ScopedMuteEntryBuilder<St, S> {
    /// Set the `onlyReposts` field (optional)
    pub fn only_reposts(mut self, value: impl Into<Option<bool>>) -> Self {
        self._fields.2 = value.into();
        self
    }
    /// Set the `onlyReposts` field to an Option value (optional)
    pub fn maybe_only_reposts(mut self, value: Option<bool>) -> Self {
        self._fields.2 = value;
        self
    }
}

impl<St, S: BosStr> ScopedMuteEntryBuilder<St, S>
where
    St: scoped_mute_entry_state::State,
    St::Subject: scoped_mute_entry_state::IsUnset,
{
    /// Set the `subject` field (required)
    pub fn subject(
        mut self,
        value: impl Into<Did<S>>,
    ) -> ScopedMuteEntryBuilder<scoped_mute_entry_state::SetSubject<St>, S> {
        self._fields.3 = Option::Some(value.into());
        ScopedMuteEntryBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> ScopedMuteEntryBuilder<St, S>
where
    St: scoped_mute_entry_state::State,
    St::Subject: scoped_mute_entry_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> ScopedMuteEntry<S> {
        ScopedMuteEntry {
            created_at: self._fields.0,
            only_quoteposts: self._fields.1.or_else(|| Some(false)),
            only_reposts: self._fields.2.or_else(|| Some(false)),
            subject: self._fields.3.unwrap(),
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(self, extra_data: BTreeMap<SmolStr, Data<S>>) -> ScopedMuteEntry<S> {
        ScopedMuteEntry {
            created_at: self._fields.0,
            only_quoteposts: self._fields.1.or_else(|| Some(false)),
            only_reposts: self._fields.2.or_else(|| Some(false)),
            subject: self._fields.3.unwrap(),
            extra_data: Some(extra_data),
        }
    }
}
