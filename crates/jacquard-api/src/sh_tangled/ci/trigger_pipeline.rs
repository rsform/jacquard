// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: sh.tangled.ci.triggerPipeline
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{CowStr, BosStr, DefaultStr, FromStaticStr};
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::string::{Did, AtUri};
use jacquard_common::types::value::Data;
use jacquard_derive::{IntoStatic, open_union};
use serde::{Serialize, Deserialize};
use crate::sh_tangled::ci::trigger::Manual;
use crate::sh_tangled::ci::trigger::PullRequest;

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct TriggerPipeline<S: BosStr = DefaultStr> {
    ///Target repository DID. Auth is checked against this repo.
    pub repo: Did<S>,
    ///Trigger metadata for this dispatch.
    pub trigger: TriggerPipelineTrigger<S>,
    ///Workflow names to run. When not provided, every dispatchable workflow is run.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub workflows: Option<Vec<S>>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}


#[open_union]
#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(tag = "$type", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub enum TriggerPipelineTrigger<S: BosStr = DefaultStr> {
    #[serde(rename = "sh.tangled.ci.trigger#manual")]
    TriggerManual(Box<Manual<S>>),
    #[serde(rename = "sh.tangled.ci.trigger#pullRequest")]
    TriggerPullRequest(Box<PullRequest<S>>),
}


#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct TriggerPipelineOutput<S: BosStr = DefaultStr> {
    ///AT-URI of the created pipeline
    pub pipeline: AtUri<S>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}


#[derive(
    Serialize,
    Deserialize,
    Debug,
    Clone,
    PartialEq,
    Eq,
    thiserror::Error,
    miette::Diagnostic
)]

#[serde(tag = "error", content = "message")]
pub enum TriggerPipelineError {
    /// Invalid request parameters
    #[serde(rename = "InvalidRequest")]
    InvalidRequest(Option<SmolStr>),
    /// Catch-all for unknown error codes.
    #[serde(untagged)]
    Other { error: SmolStr, message: Option<SmolStr> },
}

impl core::fmt::Display for TriggerPipelineError {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::InvalidRequest(msg) => {
                write!(f, "InvalidRequest")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::Other { error, message } => {
                write!(f, "{}", error)?;
                if let Some(msg) = message {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
        }
    }
}

/** Response marker for the `sh.tangled.ci.triggerPipeline` procedure.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `TriggerPipelineOutput<S>` for this endpoint.*/
pub struct TriggerPipelineResponse;
impl jacquard_common::xrpc::XrpcResp for TriggerPipelineResponse {
    const NSID: &'static str = "sh.tangled.ci.triggerPipeline";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = TriggerPipelineOutput<S>;
    type Err = TriggerPipelineError;
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for TriggerPipeline<S> {
    const NSID: &'static str = "sh.tangled.ci.triggerPipeline";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Procedure(
        "application/json",
    );
    type Response = TriggerPipelineResponse;
}

/** Endpoint marker for the `sh.tangled.ci.triggerPipeline` procedure.

Path: `/xrpc/sh.tangled.ci.triggerPipeline`. The request payload type is `TriggerPipeline<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct TriggerPipelineRequest;
impl jacquard_common::xrpc::XrpcEndpoint for TriggerPipelineRequest {
    const PATH: &'static str = "/xrpc/sh.tangled.ci.triggerPipeline";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Procedure(
        "application/json",
    );
    type Request<S: BosStr> = TriggerPipeline<S>;
    type Response = TriggerPipelineResponse;
}

pub mod trigger_pipeline_state {

    pub use crate::builder_types::{Set, Unset, IsSet, IsUnset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Repo;
        type Trigger;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Repo = Unset;
        type Trigger = Unset;
    }
    ///State transition - sets the `repo` field to Set
    pub struct SetRepo<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetRepo<St> {}
    impl<St: State> State for SetRepo<St> {
        type Repo = Set<members::repo>;
        type Trigger = St::Trigger;
    }
    ///State transition - sets the `trigger` field to Set
    pub struct SetTrigger<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetTrigger<St> {}
    impl<St: State> State for SetTrigger<St> {
        type Repo = St::Repo;
        type Trigger = Set<members::trigger>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `repo` field
        pub struct repo(());
        ///Marker type for the `trigger` field
        pub struct trigger(());
    }
}

/// Builder for constructing an instance of this type.
pub struct TriggerPipelineBuilder<
    St: trigger_pipeline_state::State,
    S: BosStr = DefaultStr,
> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<Did<S>>, Option<TriggerPipelineTrigger<S>>, Option<Vec<S>>),
    _type: PhantomData<fn() -> S>,
}

impl TriggerPipeline<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> TriggerPipelineBuilder<trigger_pipeline_state::Empty, DefaultStr> {
        TriggerPipelineBuilder::new()
    }
}

impl<S: BosStr> TriggerPipeline<S> {
    /// Create a new builder for this type
    pub fn builder() -> TriggerPipelineBuilder<trigger_pipeline_state::Empty, S> {
        TriggerPipelineBuilder::builder()
    }
}

impl TriggerPipelineBuilder<trigger_pipeline_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        TriggerPipelineBuilder {
            _state: PhantomData,
            _fields: (None, None, None),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> TriggerPipelineBuilder<trigger_pipeline_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        TriggerPipelineBuilder {
            _state: PhantomData,
            _fields: (None, None, None),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> TriggerPipelineBuilder<St, S>
where
    St: trigger_pipeline_state::State,
    St::Repo: trigger_pipeline_state::IsUnset,
{
    /// Set the `repo` field (required)
    pub fn repo(
        mut self,
        value: impl Into<Did<S>>,
    ) -> TriggerPipelineBuilder<trigger_pipeline_state::SetRepo<St>, S> {
        self._fields.0 = Option::Some(value.into());
        TriggerPipelineBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> TriggerPipelineBuilder<St, S>
where
    St: trigger_pipeline_state::State,
    St::Trigger: trigger_pipeline_state::IsUnset,
{
    /// Set the `trigger` field (required)
    pub fn trigger(
        mut self,
        value: impl Into<TriggerPipelineTrigger<S>>,
    ) -> TriggerPipelineBuilder<trigger_pipeline_state::SetTrigger<St>, S> {
        self._fields.1 = Option::Some(value.into());
        TriggerPipelineBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St: trigger_pipeline_state::State, S: BosStr> TriggerPipelineBuilder<St, S> {
    /// Set the `workflows` field (optional)
    pub fn workflows(mut self, value: impl Into<Option<Vec<S>>>) -> Self {
        self._fields.2 = value.into();
        self
    }
    /// Set the `workflows` field to an Option value (optional)
    pub fn maybe_workflows(mut self, value: Option<Vec<S>>) -> Self {
        self._fields.2 = value;
        self
    }
}

impl<St, S: BosStr> TriggerPipelineBuilder<St, S>
where
    St: trigger_pipeline_state::State,
    St::Repo: trigger_pipeline_state::IsSet,
    St::Trigger: trigger_pipeline_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> TriggerPipeline<S> {
        TriggerPipeline {
            repo: self._fields.0.unwrap(),
            trigger: self._fields.1.unwrap(),
            workflows: self._fields.2,
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(
        self,
        extra_data: BTreeMap<SmolStr, Data<S>>,
    ) -> TriggerPipeline<S> {
        TriggerPipeline {
            repo: self._fields.0.unwrap(),
            trigger: self._fields.1.unwrap(),
            workflows: self._fields.2,
            extra_data: Some(extra_data),
        }
    }
}