// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: sh.tangled.git.temp.getTag
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::deps::bytes::Bytes;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::string::Did;
use jacquard_common::types::value::Data;
use jacquard_common::{BosStr, CowStr, DefaultStr, FromStaticStr};
use jacquard_derive::{IntoStatic, open_union};
use serde::{Deserialize, Serialize};

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct GetTag<S: BosStr = DefaultStr> {
    pub repo: Did<S>,
    pub tag: S,
}

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase")]
pub struct GetTagOutput {
    pub body: Bytes,
}

#[derive(
    Serialize, Deserialize, Debug, Clone, PartialEq, Eq, thiserror::Error, miette::Diagnostic,
)]
#[serde(tag = "error", content = "message")]
pub enum GetTagError {
    /// Repository not found or access denied
    #[serde(rename = "RepoNotFound")]
    RepoNotFound(Option<SmolStr>),
    /// Tag not found
    #[serde(rename = "TagNotFound")]
    TagNotFound(Option<SmolStr>),
    /// Invalid request parameters
    #[serde(rename = "InvalidRequest")]
    InvalidRequest(Option<SmolStr>),
    /// Catch-all for unknown error codes.
    #[serde(untagged)]
    Other {
        error: SmolStr,
        message: Option<SmolStr>,
    },
}

impl core::fmt::Display for GetTagError {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::RepoNotFound(msg) => {
                write!(f, "RepoNotFound")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::TagNotFound(msg) => {
                write!(f, "TagNotFound")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::InvalidRequest(msg) => {
                write!(f, "InvalidRequest")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::Other { error, message } => {
                write!(f, "{}", error)?;
                if let Some(msg) = message {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
        }
    }
}

/** Response marker for the `sh.tangled.git.temp.getTag` query.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `GetTagOutput` for this endpoint.*/
pub struct GetTagResponse;
impl jacquard_common::xrpc::XrpcResp for GetTagResponse {
    const NSID: &'static str = "sh.tangled.git.temp.getTag";
    const ENCODING: &'static str = "*/*";
    type Output<S: BosStr> = GetTagOutput;
    type Err = GetTagError;
    fn encode_output<S: BosStr>(
        output: &Self::Output<S>,
    ) -> Result<Vec<u8>, jacquard_common::xrpc::EncodeError>
    where
        Self::Output<S>: Serialize,
    {
        Ok(output.body.to_vec())
    }
    fn decode_output<'de, S>(
        body: &'de [u8],
    ) -> Result<Self::Output<S>, jacquard_common::error::DecodeError>
    where
        S: BosStr + Deserialize<'de>,
        Self::Output<S>: Deserialize<'de>,
    {
        Ok(GetTagOutput {
            body: jacquard_common::deps::bytes::Bytes::copy_from_slice(body),
        })
    }
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for GetTag<S> {
    const NSID: &'static str = "sh.tangled.git.temp.getTag";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Query;
    type Response = GetTagResponse;
}

/** Endpoint marker for the `sh.tangled.git.temp.getTag` query.

Path: `/xrpc/sh.tangled.git.temp.getTag`. The request payload type is `GetTag<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct GetTagRequest;
impl jacquard_common::xrpc::XrpcEndpoint for GetTagRequest {
    const PATH: &'static str = "/xrpc/sh.tangled.git.temp.getTag";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Query;
    type Request<S: BosStr> = GetTag<S>;
    type Response = GetTagResponse;
}

pub mod get_tag_state {

    pub use crate::builder_types::{IsSet, IsUnset, Set, Unset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Repo;
        type Tag;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Repo = Unset;
        type Tag = Unset;
    }
    ///State transition - sets the `repo` field to Set
    pub struct SetRepo<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetRepo<St> {}
    impl<St: State> State for SetRepo<St> {
        type Repo = Set<members::repo>;
        type Tag = St::Tag;
    }
    ///State transition - sets the `tag` field to Set
    pub struct SetTag<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetTag<St> {}
    impl<St: State> State for SetTag<St> {
        type Repo = St::Repo;
        type Tag = Set<members::tag>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `repo` field
        pub struct repo(());
        ///Marker type for the `tag` field
        pub struct tag(());
    }
}

/// Builder for constructing an instance of this type.
pub struct GetTagBuilder<St: get_tag_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<Did<S>>, Option<S>),
    _type: PhantomData<fn() -> S>,
}

impl GetTag<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> GetTagBuilder<get_tag_state::Empty, DefaultStr> {
        GetTagBuilder::new()
    }
}

impl<S: BosStr> GetTag<S> {
    /// Create a new builder for this type
    pub fn builder() -> GetTagBuilder<get_tag_state::Empty, S> {
        GetTagBuilder::builder()
    }
}

impl GetTagBuilder<get_tag_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        GetTagBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> GetTagBuilder<get_tag_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        GetTagBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> GetTagBuilder<St, S>
where
    St: get_tag_state::State,
    St::Repo: get_tag_state::IsUnset,
{
    /// Set the `repo` field (required)
    pub fn repo(
        mut self,
        value: impl Into<Did<S>>,
    ) -> GetTagBuilder<get_tag_state::SetRepo<St>, S> {
        self._fields.0 = Option::Some(value.into());
        GetTagBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> GetTagBuilder<St, S>
where
    St: get_tag_state::State,
    St::Tag: get_tag_state::IsUnset,
{
    /// Set the `tag` field (required)
    pub fn tag(mut self, value: impl Into<S>) -> GetTagBuilder<get_tag_state::SetTag<St>, S> {
        self._fields.1 = Option::Some(value.into());
        GetTagBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> GetTagBuilder<St, S>
where
    St: get_tag_state::State,
    St::Repo: get_tag_state::IsSet,
    St::Tag: get_tag_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> GetTag<S> {
        GetTag {
            repo: self._fields.0.unwrap(),
            tag: self._fields.1.unwrap(),
        }
    }
}
