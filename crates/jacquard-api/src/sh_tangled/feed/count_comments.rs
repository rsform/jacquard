// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: sh.tangled.feed.countComments
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::string::AtUri;
use jacquard_common::types::value::Data;
use jacquard_common::{BosStr, DefaultStr, FromStaticStr};
use jacquard_derive::IntoStatic;
use serde::{Deserialize, Serialize};

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct CountComments<S: BosStr = DefaultStr> {
    pub subject: AtUri<S>,
}

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct CountCommentsOutput<S: BosStr = DefaultStr> {
    /// Total number of matching records.
    pub count: i64,
    /// Number of distinct authors among the matching records.
    pub distinct_authors: i64,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

/** Response marker for the `sh.tangled.feed.countComments` query.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `CountCommentsOutput<S>` for this endpoint.*/
pub struct CountCommentsResponse;
impl jacquard_common::xrpc::XrpcResp for CountCommentsResponse {
    const NSID: &'static str = "sh.tangled.feed.countComments";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = CountCommentsOutput<S>;
    type Err = jacquard_common::xrpc::GenericError;
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for CountComments<S> {
    const NSID: &'static str = "sh.tangled.feed.countComments";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Query;
    type Response = CountCommentsResponse;
}

/** Endpoint marker for the `sh.tangled.feed.countComments` query.

Path: `/xrpc/sh.tangled.feed.countComments`. The request payload type is `CountComments<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct CountCommentsRequest;
impl jacquard_common::xrpc::XrpcEndpoint for CountCommentsRequest {
    const PATH: &'static str = "/xrpc/sh.tangled.feed.countComments";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Query;
    type Request<S: BosStr> = CountComments<S>;
    type Response = CountCommentsResponse;
}

pub mod count_comments_state {

    pub use crate::builder_types::{IsSet, IsUnset, Set, Unset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Subject;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Subject = Unset;
    }
    ///State transition - sets the `subject` field to Set
    pub struct SetSubject<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetSubject<St> {}
    impl<St: State> State for SetSubject<St> {
        type Subject = Set<members::subject>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `subject` field
        pub struct subject(());
    }
}

/// Builder for constructing an instance of this type.
pub struct CountCommentsBuilder<St: count_comments_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<AtUri<S>>,),
    _type: PhantomData<fn() -> S>,
}

impl CountComments<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> CountCommentsBuilder<count_comments_state::Empty, DefaultStr> {
        CountCommentsBuilder::new()
    }
}

impl<S: BosStr> CountComments<S> {
    /// Create a new builder for this type
    pub fn builder() -> CountCommentsBuilder<count_comments_state::Empty, S> {
        CountCommentsBuilder::builder()
    }
}

impl CountCommentsBuilder<count_comments_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        CountCommentsBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> CountCommentsBuilder<count_comments_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        CountCommentsBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> CountCommentsBuilder<St, S>
where
    St: count_comments_state::State,
    St::Subject: count_comments_state::IsUnset,
{
    /// Set the `subject` field (required)
    pub fn subject(
        mut self,
        value: impl Into<AtUri<S>>,
    ) -> CountCommentsBuilder<count_comments_state::SetSubject<St>, S> {
        self._fields.0 = Option::Some(value.into());
        CountCommentsBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> CountCommentsBuilder<St, S>
where
    St: count_comments_state::State,
    St::Subject: count_comments_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> CountComments<S> {
        CountComments {
            subject: self._fields.0.unwrap(),
        }
    }
}
