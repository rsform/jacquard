// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: sh.tangled.knot.ban
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::string::Did;
use jacquard_common::types::value::Data;
use jacquard_common::{BosStr, DefaultStr, FromStaticStr};
use jacquard_derive::IntoStatic;
use serde::{Deserialize, Serialize};

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct Ban<S: BosStr = DefaultStr> {
    ///DID of the account to block
    pub subject: Did<S>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

/** Response marker for the `sh.tangled.knot.ban` procedure.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `()` for this endpoint.*/
pub struct BanResponse;
impl jacquard_common::xrpc::XrpcResp for BanResponse {
    const NSID: &'static str = "sh.tangled.knot.ban";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = ();
    type Err = jacquard_common::xrpc::GenericError;
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for Ban<S> {
    const NSID: &'static str = "sh.tangled.knot.ban";
    const METHOD: jacquard_common::xrpc::XrpcMethod =
        jacquard_common::xrpc::XrpcMethod::Procedure("application/json");
    type Response = BanResponse;
}

/** Endpoint marker for the `sh.tangled.knot.ban` procedure.

Path: `/xrpc/sh.tangled.knot.ban`. The request payload type is `Ban<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct BanRequest;
impl jacquard_common::xrpc::XrpcEndpoint for BanRequest {
    const PATH: &'static str = "/xrpc/sh.tangled.knot.ban";
    const METHOD: jacquard_common::xrpc::XrpcMethod =
        jacquard_common::xrpc::XrpcMethod::Procedure("application/json");
    type Request<S: BosStr> = Ban<S>;
    type Response = BanResponse;
}

pub mod ban_state {

    pub use crate::builder_types::{IsSet, IsUnset, Set, Unset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Subject;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Subject = Unset;
    }
    ///State transition - sets the `subject` field to Set
    pub struct SetSubject<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetSubject<St> {}
    impl<St: State> State for SetSubject<St> {
        type Subject = Set<members::subject>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `subject` field
        pub struct subject(());
    }
}

/// Builder for constructing an instance of this type.
pub struct BanBuilder<St: ban_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<Did<S>>,),
    _type: PhantomData<fn() -> S>,
}

impl Ban<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> BanBuilder<ban_state::Empty, DefaultStr> {
        BanBuilder::new()
    }
}

impl<S: BosStr> Ban<S> {
    /// Create a new builder for this type
    pub fn builder() -> BanBuilder<ban_state::Empty, S> {
        BanBuilder::builder()
    }
}

impl BanBuilder<ban_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        BanBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> BanBuilder<ban_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        BanBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> BanBuilder<St, S>
where
    St: ban_state::State,
    St::Subject: ban_state::IsUnset,
{
    /// Set the `subject` field (required)
    pub fn subject(mut self, value: impl Into<Did<S>>) -> BanBuilder<ban_state::SetSubject<St>, S> {
        self._fields.0 = Option::Some(value.into());
        BanBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> BanBuilder<St, S>
where
    St: ban_state::State,
    St::Subject: ban_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> Ban<S> {
        Ban {
            subject: self._fields.0.unwrap(),
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(self, extra_data: BTreeMap<SmolStr, Data<S>>) -> Ban<S> {
        Ban {
            subject: self._fields.0.unwrap(),
            extra_data: Some(extra_data),
        }
    }
}
