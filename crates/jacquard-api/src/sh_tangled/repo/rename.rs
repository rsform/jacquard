// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: sh.tangled.repo.rename
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::string::{Did, RecordKey, Rkey};
use jacquard_common::types::value::Data;
use jacquard_common::{BosStr, CowStr, DefaultStr, FromStaticStr};
use jacquard_derive::IntoStatic;
use serde::{Deserialize, Serialize};

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct Rename<S: BosStr = DefaultStr> {
    ///New display name of the repository
    pub name: S,
    ///DID of the repository to rename
    pub repo: Did<S>,
    ///Rkey of the sh.tangled.repo record written for the new name
    pub rkey: RecordKey<Rkey<S>>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

/** Response marker for the `sh.tangled.repo.rename` procedure.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `()` for this endpoint.*/
pub struct RenameResponse;
impl jacquard_common::xrpc::XrpcResp for RenameResponse {
    const NSID: &'static str = "sh.tangled.repo.rename";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = ();
    type Err = jacquard_common::xrpc::GenericError;
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for Rename<S> {
    const NSID: &'static str = "sh.tangled.repo.rename";
    const METHOD: jacquard_common::xrpc::XrpcMethod =
        jacquard_common::xrpc::XrpcMethod::Procedure("application/json");
    type Response = RenameResponse;
}

/** Endpoint marker for the `sh.tangled.repo.rename` procedure.

Path: `/xrpc/sh.tangled.repo.rename`. The request payload type is `Rename<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct RenameRequest;
impl jacquard_common::xrpc::XrpcEndpoint for RenameRequest {
    const PATH: &'static str = "/xrpc/sh.tangled.repo.rename";
    const METHOD: jacquard_common::xrpc::XrpcMethod =
        jacquard_common::xrpc::XrpcMethod::Procedure("application/json");
    type Request<S: BosStr> = Rename<S>;
    type Response = RenameResponse;
}

pub mod rename_state {

    pub use crate::builder_types::{IsSet, IsUnset, Set, Unset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Name;
        type Repo;
        type Rkey;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Name = Unset;
        type Repo = Unset;
        type Rkey = Unset;
    }
    ///State transition - sets the `name` field to Set
    pub struct SetName<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetName<St> {}
    impl<St: State> State for SetName<St> {
        type Name = Set<members::name>;
        type Repo = St::Repo;
        type Rkey = St::Rkey;
    }
    ///State transition - sets the `repo` field to Set
    pub struct SetRepo<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetRepo<St> {}
    impl<St: State> State for SetRepo<St> {
        type Name = St::Name;
        type Repo = Set<members::repo>;
        type Rkey = St::Rkey;
    }
    ///State transition - sets the `rkey` field to Set
    pub struct SetRkey<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetRkey<St> {}
    impl<St: State> State for SetRkey<St> {
        type Name = St::Name;
        type Repo = St::Repo;
        type Rkey = Set<members::rkey>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `name` field
        pub struct name(());
        ///Marker type for the `repo` field
        pub struct repo(());
        ///Marker type for the `rkey` field
        pub struct rkey(());
    }
}

/// Builder for constructing an instance of this type.
pub struct RenameBuilder<St: rename_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<S>, Option<Did<S>>, Option<RecordKey<Rkey<S>>>),
    _type: PhantomData<fn() -> S>,
}

impl Rename<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> RenameBuilder<rename_state::Empty, DefaultStr> {
        RenameBuilder::new()
    }
}

impl<S: BosStr> Rename<S> {
    /// Create a new builder for this type
    pub fn builder() -> RenameBuilder<rename_state::Empty, S> {
        RenameBuilder::builder()
    }
}

impl RenameBuilder<rename_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        RenameBuilder {
            _state: PhantomData,
            _fields: (None, None, None),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> RenameBuilder<rename_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        RenameBuilder {
            _state: PhantomData,
            _fields: (None, None, None),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> RenameBuilder<St, S>
where
    St: rename_state::State,
    St::Name: rename_state::IsUnset,
{
    /// Set the `name` field (required)
    pub fn name(mut self, value: impl Into<S>) -> RenameBuilder<rename_state::SetName<St>, S> {
        self._fields.0 = Option::Some(value.into());
        RenameBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> RenameBuilder<St, S>
where
    St: rename_state::State,
    St::Repo: rename_state::IsUnset,
{
    /// Set the `repo` field (required)
    pub fn repo(mut self, value: impl Into<Did<S>>) -> RenameBuilder<rename_state::SetRepo<St>, S> {
        self._fields.1 = Option::Some(value.into());
        RenameBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> RenameBuilder<St, S>
where
    St: rename_state::State,
    St::Rkey: rename_state::IsUnset,
{
    /// Set the `rkey` field (required)
    pub fn rkey(
        mut self,
        value: impl Into<RecordKey<Rkey<S>>>,
    ) -> RenameBuilder<rename_state::SetRkey<St>, S> {
        self._fields.2 = Option::Some(value.into());
        RenameBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> RenameBuilder<St, S>
where
    St: rename_state::State,
    St::Name: rename_state::IsSet,
    St::Repo: rename_state::IsSet,
    St::Rkey: rename_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> Rename<S> {
        Rename {
            name: self._fields.0.unwrap(),
            repo: self._fields.1.unwrap(),
            rkey: self._fields.2.unwrap(),
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(self, extra_data: BTreeMap<SmolStr, Data<S>>) -> Rename<S> {
        Rename {
            name: self._fields.0.unwrap(),
            repo: self._fields.1.unwrap(),
            rkey: self._fields.2.unwrap(),
            extra_data: Some(extra_data),
        }
    }
}
