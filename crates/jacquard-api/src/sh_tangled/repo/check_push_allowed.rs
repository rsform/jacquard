// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: sh.tangled.repo.checkPushAllowed
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::string::Did;
use jacquard_common::types::value::Data;
use jacquard_common::{BosStr, CowStr, DefaultStr, FromStaticStr};
use jacquard_derive::{IntoStatic, open_union};
use serde::{Deserialize, Serialize};

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct CheckPushAllowed<S: BosStr = DefaultStr> {
    pub key: S,
    pub repo: S,
}

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct CheckPushAllowedOutput<S: BosStr = DefaultStr> {
    /// Whether the key's owner may push to the repo.
    pub allowed: bool,
    /// DID the key resolved to, if a match was found.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub did: Option<Did<S>>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

#[derive(
    Serialize, Deserialize, Debug, Clone, PartialEq, Eq, thiserror::Error, miette::Diagnostic,
)]
#[serde(tag = "error", content = "message")]
pub enum CheckPushAllowedError {
    /// The repo could not be resolved on this knot.
    #[serde(rename = "RepoNotFound")]
    RepoNotFound(#[serde(skip_serializing_if = "Option::is_none")] Option<SmolStr>),
    /// Missing or malformed parameters.
    #[serde(rename = "InvalidRequest")]
    InvalidRequest(#[serde(skip_serializing_if = "Option::is_none")] Option<SmolStr>),
    /// Catch-all for unknown error codes.
    #[serde(untagged)]
    Other {
        error: SmolStr,
        #[serde(skip_serializing_if = "Option::is_none")]
        message: Option<SmolStr>,
    },
}

impl core::fmt::Display for CheckPushAllowedError {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::RepoNotFound(msg) => {
                write!(f, "RepoNotFound")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::InvalidRequest(msg) => {
                write!(f, "InvalidRequest")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::Other { error, message } => {
                write!(f, "{}", error)?;
                if let Some(msg) = message {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
        }
    }
}

/** Response marker for the `sh.tangled.repo.checkPushAllowed` query.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `CheckPushAllowedOutput<S>` for this endpoint.*/
pub struct CheckPushAllowedResponse;
impl jacquard_common::xrpc::XrpcResp for CheckPushAllowedResponse {
    const NSID: &'static str = "sh.tangled.repo.checkPushAllowed";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = CheckPushAllowedOutput<S>;
    type Err = CheckPushAllowedError;
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for CheckPushAllowed<S> {
    const NSID: &'static str = "sh.tangled.repo.checkPushAllowed";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Query;
    type Response = CheckPushAllowedResponse;
}

/** Endpoint marker for the `sh.tangled.repo.checkPushAllowed` query.

Path: `/xrpc/sh.tangled.repo.checkPushAllowed`. The request payload type is `CheckPushAllowed<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct CheckPushAllowedRequest;
impl jacquard_common::xrpc::XrpcEndpoint for CheckPushAllowedRequest {
    const PATH: &'static str = "/xrpc/sh.tangled.repo.checkPushAllowed";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Query;
    type Request<S: BosStr> = CheckPushAllowed<S>;
    type Response = CheckPushAllowedResponse;
}

pub mod check_push_allowed_state {

    pub use crate::builder_types::{IsSet, IsUnset, Set, Unset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Key;
        type Repo;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Key = Unset;
        type Repo = Unset;
    }
    ///State transition - sets the `key` field to Set
    pub struct SetKey<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetKey<St> {}
    impl<St: State> State for SetKey<St> {
        type Key = Set<members::key>;
        type Repo = St::Repo;
    }
    ///State transition - sets the `repo` field to Set
    pub struct SetRepo<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetRepo<St> {}
    impl<St: State> State for SetRepo<St> {
        type Key = St::Key;
        type Repo = Set<members::repo>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `key` field
        pub struct key(());
        ///Marker type for the `repo` field
        pub struct repo(());
    }
}

/// Builder for constructing an instance of this type.
pub struct CheckPushAllowedBuilder<St: check_push_allowed_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<S>, Option<S>),
    _type: PhantomData<fn() -> S>,
}

impl CheckPushAllowed<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> CheckPushAllowedBuilder<check_push_allowed_state::Empty, DefaultStr> {
        CheckPushAllowedBuilder::new()
    }
}

impl<S: BosStr> CheckPushAllowed<S> {
    /// Create a new builder for this type
    pub fn builder() -> CheckPushAllowedBuilder<check_push_allowed_state::Empty, S> {
        CheckPushAllowedBuilder::builder()
    }
}

impl CheckPushAllowedBuilder<check_push_allowed_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        CheckPushAllowedBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> CheckPushAllowedBuilder<check_push_allowed_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        CheckPushAllowedBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> CheckPushAllowedBuilder<St, S>
where
    St: check_push_allowed_state::State,
    St::Key: check_push_allowed_state::IsUnset,
{
    /// Set the `key` field (required)
    pub fn key(
        mut self,
        value: impl Into<S>,
    ) -> CheckPushAllowedBuilder<check_push_allowed_state::SetKey<St>, S> {
        self._fields.0 = Option::Some(value.into());
        CheckPushAllowedBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> CheckPushAllowedBuilder<St, S>
where
    St: check_push_allowed_state::State,
    St::Repo: check_push_allowed_state::IsUnset,
{
    /// Set the `repo` field (required)
    pub fn repo(
        mut self,
        value: impl Into<S>,
    ) -> CheckPushAllowedBuilder<check_push_allowed_state::SetRepo<St>, S> {
        self._fields.1 = Option::Some(value.into());
        CheckPushAllowedBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> CheckPushAllowedBuilder<St, S>
where
    St: check_push_allowed_state::State,
    St::Key: check_push_allowed_state::IsSet,
    St::Repo: check_push_allowed_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> CheckPushAllowed<S> {
        CheckPushAllowed {
            key: self._fields.0.unwrap(),
            repo: self._fields.1.unwrap(),
        }
    }
}
