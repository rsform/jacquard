// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: sh.tangled.repo.reserveKey
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{CowStr, BosStr, DefaultStr, FromStaticStr};
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::string::Did;
use jacquard_common::types::value::Data;
use jacquard_derive::IntoStatic;
use serde::{Serialize, Deserialize};

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct ReserveKey<S: BosStr = DefaultStr> {
    ///The did:web identity the repository will use.
    pub repo_did: Did<S>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}


#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct ReserveKeyOutput<S: BosStr = DefaultStr> {
    ///Multibase-encoded public key to publish in the did:web document.
    pub key: S,
    pub repo_did: Did<S>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

/** Response marker for the `sh.tangled.repo.reserveKey` procedure.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `ReserveKeyOutput<S>` for this endpoint.*/
pub struct ReserveKeyResponse;
impl jacquard_common::xrpc::XrpcResp for ReserveKeyResponse {
    const NSID: &'static str = "sh.tangled.repo.reserveKey";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = ReserveKeyOutput<S>;
    type Err = jacquard_common::xrpc::GenericError;
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for ReserveKey<S> {
    const NSID: &'static str = "sh.tangled.repo.reserveKey";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Procedure(
        "application/json",
    );
    type Response = ReserveKeyResponse;
}

/** Endpoint marker for the `sh.tangled.repo.reserveKey` procedure.

Path: `/xrpc/sh.tangled.repo.reserveKey`. The request payload type is `ReserveKey<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct ReserveKeyRequest;
impl jacquard_common::xrpc::XrpcEndpoint for ReserveKeyRequest {
    const PATH: &'static str = "/xrpc/sh.tangled.repo.reserveKey";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Procedure(
        "application/json",
    );
    type Request<S: BosStr> = ReserveKey<S>;
    type Response = ReserveKeyResponse;
}

pub mod reserve_key_state {

    pub use crate::builder_types::{Set, Unset, IsSet, IsUnset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type RepoDid;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type RepoDid = Unset;
    }
    ///State transition - sets the `repo_did` field to Set
    pub struct SetRepoDid<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetRepoDid<St> {}
    impl<St: State> State for SetRepoDid<St> {
        type RepoDid = Set<members::repo_did>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `repo_did` field
        pub struct repo_did(());
    }
}

/// Builder for constructing an instance of this type.
pub struct ReserveKeyBuilder<St: reserve_key_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<Did<S>>,),
    _type: PhantomData<fn() -> S>,
}

impl ReserveKey<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> ReserveKeyBuilder<reserve_key_state::Empty, DefaultStr> {
        ReserveKeyBuilder::new()
    }
}

impl<S: BosStr> ReserveKey<S> {
    /// Create a new builder for this type
    pub fn builder() -> ReserveKeyBuilder<reserve_key_state::Empty, S> {
        ReserveKeyBuilder::builder()
    }
}

impl ReserveKeyBuilder<reserve_key_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        ReserveKeyBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> ReserveKeyBuilder<reserve_key_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        ReserveKeyBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> ReserveKeyBuilder<St, S>
where
    St: reserve_key_state::State,
    St::RepoDid: reserve_key_state::IsUnset,
{
    /// Set the `repoDid` field (required)
    pub fn repo_did(
        mut self,
        value: impl Into<Did<S>>,
    ) -> ReserveKeyBuilder<reserve_key_state::SetRepoDid<St>, S> {
        self._fields.0 = Option::Some(value.into());
        ReserveKeyBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> ReserveKeyBuilder<St, S>
where
    St: reserve_key_state::State,
    St::RepoDid: reserve_key_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> ReserveKey<S> {
        ReserveKey {
            repo_did: self._fields.0.unwrap(),
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(
        self,
        extra_data: BTreeMap<SmolStr, Data<S>>,
    ) -> ReserveKey<S> {
        ReserveKey {
            repo_did: self._fields.0.unwrap(),
            extra_data: Some(extra_data),
        }
    }
}