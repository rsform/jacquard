// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: events.smokesignal.calendar.acceptance
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{CowStr, BosStr, DefaultStr, FromStaticStr};

#[allow(unused_imports)]
use jacquard_common::deps::codegen::unicode_segmentation::UnicodeSegmentation;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::collection::{Collection, RecordError};
use jacquard_common::types::string::{AtUri, Cid};
use jacquard_common::types::uri::{RecordUri, UriError};
use jacquard_common::types::value::Data;
use jacquard_common::xrpc::XrpcResp;
use jacquard_derive::{IntoStatic, lexicon};
use jacquard_lexicon::lexicon::LexiconDoc;
use jacquard_lexicon::schema::LexiconSchema;

#[allow(unused_imports)]
use jacquard_lexicon::validation::{ConstraintError, ValidationPath};
use serde::{Serialize, Deserialize};
/// A cryptographic proof record that contains RSVP acceptance data.

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    rename = "events.smokesignal.calendar.acceptance",
    tag = "$type",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct Acceptance<S: BosStr = DefaultStr> {
    ///The CID (Content Identifier) of the rsvp that this proof validates.
    pub cid: Cid<S>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

/// Typed wrapper for GetRecord response with this collection's record type.

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase")]
pub struct AcceptanceGetRecordOutput<S: BosStr = DefaultStr> {
    #[serde(skip_serializing_if = "Option::is_none")]
    pub cid: Option<Cid<S>>,
    pub uri: AtUri<S>,
    pub value: Acceptance<S>,
}

impl<S: BosStr> Acceptance<S> {
    pub fn uri(uri: S) -> Result<RecordUri<S, AcceptanceRecord>, UriError> {
        RecordUri::try_from_uri(AtUri::new(uri)?)
    }
}

/// Marker type for deserializing records from this collection.

#[derive(Debug, Serialize, Deserialize)]
pub struct AcceptanceRecord;
impl XrpcResp for AcceptanceRecord {
    const NSID: &'static str = "events.smokesignal.calendar.acceptance";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = AcceptanceGetRecordOutput<S>;
    type Err = RecordError;
}

impl<S: BosStr> From<AcceptanceGetRecordOutput<S>> for Acceptance<S> {
    fn from(output: AcceptanceGetRecordOutput<S>) -> Self {
        output.value
    }
}

impl<S: BosStr> Collection for Acceptance<S> {
    const NSID: &'static str = "events.smokesignal.calendar.acceptance";
    type Record = AcceptanceRecord;
}

impl Collection for AcceptanceRecord {
    const NSID: &'static str = "events.smokesignal.calendar.acceptance";
    type Record = AcceptanceRecord;
}

impl<S: BosStr> LexiconSchema for Acceptance<S> {
    fn nsid() -> &'static str {
        "events.smokesignal.calendar.acceptance"
    }
    fn def_name() -> &'static str {
        "main"
    }
    fn lexicon_doc() -> LexiconDoc<'static> {
        lexicon_doc_events_smokesignal_calendar_acceptance()
    }
    fn validate(&self) -> Result<(), ConstraintError> {
        Ok(())
    }
}

pub mod acceptance_state {

    pub use crate::builder_types::{Set, Unset, IsSet, IsUnset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Cid;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Cid = Unset;
    }
    ///State transition - sets the `cid` field to Set
    pub struct SetCid<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetCid<St> {}
    impl<St: State> State for SetCid<St> {
        type Cid = Set<members::cid>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `cid` field
        pub struct cid(());
    }
}

/// Builder for constructing an instance of this type.
pub struct AcceptanceBuilder<St: acceptance_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<Cid<S>>,),
    _type: PhantomData<fn() -> S>,
}

impl Acceptance<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> AcceptanceBuilder<acceptance_state::Empty, DefaultStr> {
        AcceptanceBuilder::new()
    }
}

impl<S: BosStr> Acceptance<S> {
    /// Create a new builder for this type
    pub fn builder() -> AcceptanceBuilder<acceptance_state::Empty, S> {
        AcceptanceBuilder::builder()
    }
}

impl AcceptanceBuilder<acceptance_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        AcceptanceBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> AcceptanceBuilder<acceptance_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        AcceptanceBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> AcceptanceBuilder<St, S>
where
    St: acceptance_state::State,
    St::Cid: acceptance_state::IsUnset,
{
    /// Set the `cid` field (required)
    pub fn cid(
        mut self,
        value: impl Into<Cid<S>>,
    ) -> AcceptanceBuilder<acceptance_state::SetCid<St>, S> {
        self._fields.0 = Option::Some(value.into());
        AcceptanceBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> AcceptanceBuilder<St, S>
where
    St: acceptance_state::State,
    St::Cid: acceptance_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> Acceptance<S> {
        Acceptance {
            cid: self._fields.0.unwrap(),
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(
        self,
        extra_data: BTreeMap<SmolStr, Data<S>>,
    ) -> Acceptance<S> {
        Acceptance {
            cid: self._fields.0.unwrap(),
            extra_data: Some(extra_data),
        }
    }
}

fn lexicon_doc_events_smokesignal_calendar_acceptance() -> LexiconDoc<'static> {
    #[allow(unused_imports)]
    use jacquard_common::{CowStr, deps::smol_str::SmolStr, types::blob::MimeType};
    use jacquard_lexicon::lexicon::*;
    use alloc::collections::BTreeMap;
    LexiconDoc {
        lexicon: Lexicon::Lexicon1,
        id: CowStr::new_static("events.smokesignal.calendar.acceptance"),
        defs: {
            let mut map = BTreeMap::new();
            map.insert(
                SmolStr::new_static("main"),
                LexUserType::Record(LexRecord {
                    description: Some(
                        CowStr::new_static(
                            "A cryptographic proof record that contains RSVP acceptance data.",
                        ),
                    ),
                    key: Some(CowStr::new_static("tid")),
                    record: LexRecordRecord::Object(LexObject {
                        required: Some(vec![SmolStr::new_static("cid")]),
                        properties: {
                            #[allow(unused_mut)]
                            let mut map = BTreeMap::new();
                            map.insert(
                                SmolStr::new_static("cid"),
                                LexObjectProperty::String(LexString {
                                    description: Some(
                                        CowStr::new_static(
                                            "The CID (Content Identifier) of the rsvp that this proof validates.",
                                        ),
                                    ),
                                    format: Some(LexStringFormat::Cid),
                                    ..Default::default()
                                }),
                            );
                            map
                        },
                        ..Default::default()
                    }),
                    ..Default::default()
                }),
            );
            map
        },
        ..Default::default()
    }
}