// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: ooo.bsky.hidden.encryptedBlobs
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{BosStr, DefaultStr, FromStaticStr};

#[allow(unused_imports)]
use jacquard_common::deps::codegen::unicode_segmentation::UnicodeSegmentation;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::blob::BlobRef;
use jacquard_common::types::value::Data;
use jacquard_derive::IntoStatic;
use jacquard_lexicon::lexicon::LexiconDoc;
use jacquard_lexicon::schema::LexiconSchema;

#[allow(unused_imports)]
use jacquard_lexicon::validation::{ConstraintError, ValidationPath};
use serde::{Serialize, Deserialize};

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct EncryptedBlobs<S: BosStr = DefaultStr> {
    pub blobs: Vec<BlobRef<S>>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

impl<S: BosStr> LexiconSchema for EncryptedBlobs<S> {
    fn nsid() -> &'static str {
        "ooo.bsky.hidden.encryptedBlobs"
    }
    fn def_name() -> &'static str {
        "main"
    }
    fn lexicon_doc() -> LexiconDoc<'static> {
        lexicon_doc_ooo_bsky_hidden_encryptedBlobs()
    }
    fn validate(&self) -> Result<(), ConstraintError> {
        Ok(())
    }
}

pub mod encrypted_blobs_state {

    pub use crate::builder_types::{Set, Unset, IsSet, IsUnset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Blobs;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Blobs = Unset;
    }
    ///State transition - sets the `blobs` field to Set
    pub struct SetBlobs<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetBlobs<St> {}
    impl<St: State> State for SetBlobs<St> {
        type Blobs = Set<members::blobs>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `blobs` field
        pub struct blobs(());
    }
}

/// Builder for constructing an instance of this type.
pub struct EncryptedBlobsBuilder<
    St: encrypted_blobs_state::State,
    S: BosStr = DefaultStr,
> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<Vec<BlobRef<S>>>,),
    _type: PhantomData<fn() -> S>,
}

impl EncryptedBlobs<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> EncryptedBlobsBuilder<encrypted_blobs_state::Empty, DefaultStr> {
        EncryptedBlobsBuilder::new()
    }
}

impl<S: BosStr> EncryptedBlobs<S> {
    /// Create a new builder for this type
    pub fn builder() -> EncryptedBlobsBuilder<encrypted_blobs_state::Empty, S> {
        EncryptedBlobsBuilder::builder()
    }
}

impl EncryptedBlobsBuilder<encrypted_blobs_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        EncryptedBlobsBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> EncryptedBlobsBuilder<encrypted_blobs_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        EncryptedBlobsBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> EncryptedBlobsBuilder<St, S>
where
    St: encrypted_blobs_state::State,
    St::Blobs: encrypted_blobs_state::IsUnset,
{
    /// Set the `blobs` field (required)
    pub fn blobs(
        mut self,
        value: impl Into<Vec<BlobRef<S>>>,
    ) -> EncryptedBlobsBuilder<encrypted_blobs_state::SetBlobs<St>, S> {
        self._fields.0 = Option::Some(value.into());
        EncryptedBlobsBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> EncryptedBlobsBuilder<St, S>
where
    St: encrypted_blobs_state::State,
    St::Blobs: encrypted_blobs_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> EncryptedBlobs<S> {
        EncryptedBlobs {
            blobs: self._fields.0.unwrap(),
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(
        self,
        extra_data: BTreeMap<SmolStr, Data<S>>,
    ) -> EncryptedBlobs<S> {
        EncryptedBlobs {
            blobs: self._fields.0.unwrap(),
            extra_data: Some(extra_data),
        }
    }
}

fn lexicon_doc_ooo_bsky_hidden_encryptedBlobs() -> LexiconDoc<'static> {
    #[allow(unused_imports)]
    use jacquard_common::{CowStr, deps::smol_str::SmolStr, types::blob::MimeType};
    use jacquard_lexicon::lexicon::*;
    use alloc::collections::BTreeMap;
    LexiconDoc {
        lexicon: Lexicon::Lexicon1,
        id: CowStr::new_static("ooo.bsky.hidden.encryptedBlobs"),
        defs: {
            let mut map = BTreeMap::new();
            map.insert(
                SmolStr::new_static("main"),
                LexUserType::Object(LexObject {
                    required: Some(vec![SmolStr::new_static("blobs")]),
                    properties: {
                        #[allow(unused_mut)]
                        let mut map = BTreeMap::new();
                        map.insert(
                            SmolStr::new_static("blobs"),
                            LexObjectProperty::Array(LexArray {
                                items: LexArrayItem::Blob(LexBlob { ..Default::default() }),
                                ..Default::default()
                            }),
                        );
                        map
                    },
                    ..Default::default()
                }),
            );
            map
        },
        ..Default::default()
    }
}