// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: app.chavatar.schedules
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{CowStr, BosStr, DefaultStr, FromStaticStr};

#[allow(unused_imports)]
use jacquard_common::deps::codegen::unicode_segmentation::UnicodeSegmentation;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::collection::{Collection, RecordError};
use jacquard_common::types::string::{AtUri, Cid, Datetime};
use jacquard_common::types::uri::{RecordUri, UriError};
use jacquard_common::types::value::Data;
use jacquard_common::xrpc::XrpcResp;
use jacquard_derive::{IntoStatic, lexicon};
use jacquard_lexicon::lexicon::LexiconDoc;
use jacquard_lexicon::schema::LexiconSchema;

#[allow(unused_imports)]
use jacquard_lexicon::validation::{ConstraintError, ValidationPath};
use serde::{Serialize, Deserialize};
use crate::app_chavatar::settings::RotationSchedule;
/// The collection of rotation schedules defined by a user.

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    rename = "app.chavatar.schedules",
    tag = "$type",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct Schedules<S: BosStr = DefaultStr> {
    ///Ordered list of rotation schedules. Active schedule is selected by kind specificity: timed(2) > period(1) > default(0). Ties broken lexicographically by id.
    pub schedules: Vec<RotationSchedule<S>>,
    ///The timestamp when this record was last updated.
    pub updated_at: Datetime,
    #[serde(
        flatten,
        default,
        deserialize_with = "deserialize_schedules_extra_data",
        skip_serializing_if = "Option::is_none"
    )]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

/// Typed wrapper for GetRecord response with this collection's record type.

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase")]
pub struct SchedulesGetRecordOutput<S: BosStr = DefaultStr> {
    #[serde(skip_serializing_if = "Option::is_none")]
    pub cid: Option<Cid<S>>,
    pub uri: AtUri<S>,
    pub value: Schedules<S>,
}

impl<S: BosStr> Schedules<S> {
    pub fn uri(uri: S) -> Result<RecordUri<S, SchedulesRecord>, UriError> {
        RecordUri::try_from_uri(AtUri::new(uri)?)
    }
}

/// Marker type for deserializing records from this collection.

#[derive(Debug, Serialize, Deserialize)]
pub struct SchedulesRecord;
impl XrpcResp for SchedulesRecord {
    const NSID: &'static str = "app.chavatar.schedules";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = SchedulesGetRecordOutput<S>;
    type Err = RecordError;
}

impl<S: BosStr> From<SchedulesGetRecordOutput<S>> for Schedules<S> {
    fn from(output: SchedulesGetRecordOutput<S>) -> Self {
        output.value
    }
}

impl<S: BosStr> Collection for Schedules<S> {
    const NSID: &'static str = "app.chavatar.schedules";
    type Record = SchedulesRecord;
}

impl Collection for SchedulesRecord {
    const NSID: &'static str = "app.chavatar.schedules";
    type Record = SchedulesRecord;
}

impl<S: BosStr> LexiconSchema for Schedules<S> {
    fn nsid() -> &'static str {
        "app.chavatar.schedules"
    }
    fn def_name() -> &'static str {
        "main"
    }
    fn lexicon_doc() -> LexiconDoc<'static> {
        lexicon_doc_app_chavatar_schedules()
    }
    fn validate(&self) -> Result<(), ConstraintError> {
        Ok(())
    }
}

fn deserialize_schedules_extra_data<'de, S, D>(
    deserializer: D,
) -> Result<Option<BTreeMap<SmolStr, Data<S>>>, D::Error>
where
    S: BosStr + serde::Deserialize<'de>,
    D: serde::Deserializer<'de>,
{
    let mut data = <Option<
        BTreeMap<SmolStr, Data<S>>,
    > as serde::Deserialize<'de>>::deserialize(deserializer)?;
    if let Some(extra_data) = &mut data {
        extra_data.remove("$type");
        if extra_data.is_empty() {
            data = None;
        }
    }
    Ok(data)
}

pub mod schedules_state {

    pub use crate::builder_types::{Set, Unset, IsSet, IsUnset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Schedules;
        type UpdatedAt;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Schedules = Unset;
        type UpdatedAt = Unset;
    }
    ///State transition - sets the `schedules` field to Set
    pub struct SetSchedules<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetSchedules<St> {}
    impl<St: State> State for SetSchedules<St> {
        type Schedules = Set<members::schedules>;
        type UpdatedAt = St::UpdatedAt;
    }
    ///State transition - sets the `updated_at` field to Set
    pub struct SetUpdatedAt<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetUpdatedAt<St> {}
    impl<St: State> State for SetUpdatedAt<St> {
        type Schedules = St::Schedules;
        type UpdatedAt = Set<members::updated_at>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `schedules` field
        pub struct schedules(());
        ///Marker type for the `updated_at` field
        pub struct updated_at(());
    }
}

/// Builder for constructing an instance of this type.
pub struct SchedulesBuilder<St: schedules_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<Vec<RotationSchedule<S>>>, Option<Datetime>),
    _type: PhantomData<fn() -> S>,
}

impl Schedules<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> SchedulesBuilder<schedules_state::Empty, DefaultStr> {
        SchedulesBuilder::new()
    }
}

impl<S: BosStr> Schedules<S> {
    /// Create a new builder for this type
    pub fn builder() -> SchedulesBuilder<schedules_state::Empty, S> {
        SchedulesBuilder::builder()
    }
}

impl SchedulesBuilder<schedules_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        SchedulesBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> SchedulesBuilder<schedules_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        SchedulesBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> SchedulesBuilder<St, S>
where
    St: schedules_state::State,
    St::Schedules: schedules_state::IsUnset,
{
    /// Set the `schedules` field (required)
    pub fn schedules(
        mut self,
        value: impl Into<Vec<RotationSchedule<S>>>,
    ) -> SchedulesBuilder<schedules_state::SetSchedules<St>, S> {
        self._fields.0 = Option::Some(value.into());
        SchedulesBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> SchedulesBuilder<St, S>
where
    St: schedules_state::State,
    St::UpdatedAt: schedules_state::IsUnset,
{
    /// Set the `updatedAt` field (required)
    pub fn updated_at(
        mut self,
        value: impl Into<Datetime>,
    ) -> SchedulesBuilder<schedules_state::SetUpdatedAt<St>, S> {
        self._fields.1 = Option::Some(value.into());
        SchedulesBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> SchedulesBuilder<St, S>
where
    St: schedules_state::State,
    St::Schedules: schedules_state::IsSet,
    St::UpdatedAt: schedules_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> Schedules<S> {
        Schedules {
            schedules: self._fields.0.unwrap(),
            updated_at: self._fields.1.unwrap(),
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(
        self,
        extra_data: BTreeMap<SmolStr, Data<S>>,
    ) -> Schedules<S> {
        Schedules {
            schedules: self._fields.0.unwrap(),
            updated_at: self._fields.1.unwrap(),
            extra_data: Some(extra_data),
        }
    }
}

fn lexicon_doc_app_chavatar_schedules() -> LexiconDoc<'static> {
    #[allow(unused_imports)]
    use jacquard_common::{CowStr, deps::smol_str::SmolStr, types::blob::MimeType};
    use jacquard_lexicon::lexicon::*;
    use alloc::collections::BTreeMap;
    LexiconDoc {
        lexicon: Lexicon::Lexicon1,
        id: CowStr::new_static("app.chavatar.schedules"),
        defs: {
            let mut map = BTreeMap::new();
            map.insert(
                SmolStr::new_static("main"),
                LexUserType::Record(LexRecord {
                    description: Some(
                        CowStr::new_static(
                            "The collection of rotation schedules defined by a user.",
                        ),
                    ),
                    key: Some(CowStr::new_static("literal:self")),
                    record: LexRecordRecord::Object(LexObject {
                        required: Some(
                            vec![
                                SmolStr::new_static("schedules"),
                                SmolStr::new_static("updatedAt")
                            ],
                        ),
                        properties: {
                            #[allow(unused_mut)]
                            let mut map = BTreeMap::new();
                            map.insert(
                                SmolStr::new_static("schedules"),
                                LexObjectProperty::Array(LexArray {
                                    description: Some(
                                        CowStr::new_static(
                                            "Ordered list of rotation schedules. Active schedule is selected by kind specificity: timed(2) > period(1) > default(0). Ties broken lexicographically by id.",
                                        ),
                                    ),
                                    items: LexArrayItem::Ref(LexRef {
                                        r#ref: CowStr::new_static(
                                            "app.chavatar.settings#rotationSchedule",
                                        ),
                                        ..Default::default()
                                    }),
                                    ..Default::default()
                                }),
                            );
                            map.insert(
                                SmolStr::new_static("updatedAt"),
                                LexObjectProperty::String(LexString {
                                    description: Some(
                                        CowStr::new_static(
                                            "The timestamp when this record was last updated.",
                                        ),
                                    ),
                                    format: Some(LexStringFormat::Datetime),
                                    ..Default::default()
                                }),
                            );
                            map
                        },
                        ..Default::default()
                    }),
                    ..Default::default()
                }),
            );
            map
        },
        ..Default::default()
    }
}