// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: app.lexidraw.sceneRevision
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{CowStr, BosStr, DefaultStr, FromStaticStr};

#[allow(unused_imports)]
use jacquard_common::deps::codegen::unicode_segmentation::UnicodeSegmentation;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::blob::BlobRef;
use jacquard_common::types::collection::{Collection, RecordError};
use jacquard_common::types::string::{AtUri, Cid, Datetime};
use jacquard_common::types::uri::{RecordUri, UriError};
use jacquard_common::types::value::Data;
use jacquard_common::xrpc::XrpcResp;
use jacquard_derive::{IntoStatic, lexicon};
use jacquard_lexicon::lexicon::LexiconDoc;
use jacquard_lexicon::schema::LexiconSchema;

#[allow(unused_imports)]
use jacquard_lexicon::validation::{ConstraintError, ValidationPath};
use serde::{Serialize, Deserialize};
use crate::com_atproto::repo::strong_ref::StrongRef;
/// An immutable checkpoint of a app.lexidraw.scene record. Captured periodically alongside the mutable scene record; the scene blob is content-addressed and shared with the scene record, so no data is duplicated.

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    rename = "app.lexidraw.sceneRevision",
    tag = "$type",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct SceneRevision<S: BosStr = DefaultStr> {
    pub created_at: Datetime,
    ///Gzipped Excalidraw scene JSON (serializeAsJSON output, gzip level 6), as captured at checkpoint time.
    pub scene: BlobRef<S>,
    ///Strong reference (uri + record cid) to the parent app.lexidraw.scene record at checkpoint time.
    pub subject: StrongRef<S>,
    ///WebP preview of the scene (max 1024px on the longest side).
    #[serde(skip_serializing_if = "Option::is_none")]
    pub thumb: Option<BlobRef<S>>,
    #[serde(
        flatten,
        default,
        deserialize_with = "deserialize_scene_revision_extra_data",
        skip_serializing_if = "Option::is_none"
    )]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

/// Typed wrapper for GetRecord response with this collection's record type.

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase")]
pub struct SceneRevisionGetRecordOutput<S: BosStr = DefaultStr> {
    #[serde(skip_serializing_if = "Option::is_none")]
    pub cid: Option<Cid<S>>,
    pub uri: AtUri<S>,
    pub value: SceneRevision<S>,
}

impl<S: BosStr> SceneRevision<S> {
    pub fn uri(uri: S) -> Result<RecordUri<S, SceneRevisionRecord>, UriError> {
        RecordUri::try_from_uri(AtUri::new(uri)?)
    }
}

/// Marker type for deserializing records from this collection.

#[derive(Debug, Serialize, Deserialize)]
pub struct SceneRevisionRecord;
impl XrpcResp for SceneRevisionRecord {
    const NSID: &'static str = "app.lexidraw.sceneRevision";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = SceneRevisionGetRecordOutput<S>;
    type Err = RecordError;
}

impl<S: BosStr> From<SceneRevisionGetRecordOutput<S>> for SceneRevision<S> {
    fn from(output: SceneRevisionGetRecordOutput<S>) -> Self {
        output.value
    }
}

impl<S: BosStr> Collection for SceneRevision<S> {
    const NSID: &'static str = "app.lexidraw.sceneRevision";
    type Record = SceneRevisionRecord;
}

impl Collection for SceneRevisionRecord {
    const NSID: &'static str = "app.lexidraw.sceneRevision";
    type Record = SceneRevisionRecord;
}

impl<S: BosStr> LexiconSchema for SceneRevision<S> {
    fn nsid() -> &'static str {
        "app.lexidraw.sceneRevision"
    }
    fn def_name() -> &'static str {
        "main"
    }
    fn lexicon_doc() -> LexiconDoc<'static> {
        lexicon_doc_app_lexidraw_sceneRevision()
    }
    fn validate(&self) -> Result<(), ConstraintError> {
        {
            let value = &self.scene;
            {
                let size = value.blob().size;
                if size > 50000000usize {
                    return Err(ConstraintError::BlobTooLarge {
                        path: ValidationPath::from_field("scene"),
                        max: 50000000usize,
                        actual: size,
                    });
                }
            }
        }
        {
            let value = &self.scene;
            {
                let mime = value.blob().mime_type.as_str();
                let accepted: &[&str] = &["application/gzip"];
                let matched = accepted
                    .iter()
                    .any(|pattern| {
                        if *pattern == "*/*" {
                            true
                        } else if pattern.ends_with("/*") {
                            let prefix = &pattern[..pattern.len() - 2];
                            mime.starts_with(prefix)
                                && mime.as_bytes().get(prefix.len()) == Some(&b'/')
                        } else {
                            mime == *pattern
                        }
                    });
                if !matched {
                    return Err(ConstraintError::BlobMimeTypeNotAccepted {
                        path: ValidationPath::from_field("scene"),
                        accepted: vec!["application/gzip".to_string()],
                        actual: mime.to_string(),
                    });
                }
            }
        }
        if let Some(ref value) = self.thumb {
            {
                let size = value.blob().size;
                if size > 2000000usize {
                    return Err(ConstraintError::BlobTooLarge {
                        path: ValidationPath::from_field("thumb"),
                        max: 2000000usize,
                        actual: size,
                    });
                }
            }
        }
        if let Some(ref value) = self.thumb {
            {
                let mime = value.blob().mime_type.as_str();
                let accepted: &[&str] = &["image/webp"];
                let matched = accepted
                    .iter()
                    .any(|pattern| {
                        if *pattern == "*/*" {
                            true
                        } else if pattern.ends_with("/*") {
                            let prefix = &pattern[..pattern.len() - 2];
                            mime.starts_with(prefix)
                                && mime.as_bytes().get(prefix.len()) == Some(&b'/')
                        } else {
                            mime == *pattern
                        }
                    });
                if !matched {
                    return Err(ConstraintError::BlobMimeTypeNotAccepted {
                        path: ValidationPath::from_field("thumb"),
                        accepted: vec!["image/webp".to_string()],
                        actual: mime.to_string(),
                    });
                }
            }
        }
        Ok(())
    }
}

fn deserialize_scene_revision_extra_data<'de, S, D>(
    deserializer: D,
) -> Result<Option<BTreeMap<SmolStr, Data<S>>>, D::Error>
where
    S: BosStr + serde::Deserialize<'de>,
    D: serde::Deserializer<'de>,
{
    let mut data = <Option<
        BTreeMap<SmolStr, Data<S>>,
    > as serde::Deserialize<'de>>::deserialize(deserializer)?;
    if let Some(extra_data) = &mut data {
        extra_data.remove("$type");
        if extra_data.is_empty() {
            data = None;
        }
    }
    Ok(data)
}

pub mod scene_revision_state {

    pub use crate::builder_types::{Set, Unset, IsSet, IsUnset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type CreatedAt;
        type Scene;
        type Subject;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type CreatedAt = Unset;
        type Scene = Unset;
        type Subject = Unset;
    }
    ///State transition - sets the `created_at` field to Set
    pub struct SetCreatedAt<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetCreatedAt<St> {}
    impl<St: State> State for SetCreatedAt<St> {
        type CreatedAt = Set<members::created_at>;
        type Scene = St::Scene;
        type Subject = St::Subject;
    }
    ///State transition - sets the `scene` field to Set
    pub struct SetScene<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetScene<St> {}
    impl<St: State> State for SetScene<St> {
        type CreatedAt = St::CreatedAt;
        type Scene = Set<members::scene>;
        type Subject = St::Subject;
    }
    ///State transition - sets the `subject` field to Set
    pub struct SetSubject<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetSubject<St> {}
    impl<St: State> State for SetSubject<St> {
        type CreatedAt = St::CreatedAt;
        type Scene = St::Scene;
        type Subject = Set<members::subject>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `created_at` field
        pub struct created_at(());
        ///Marker type for the `scene` field
        pub struct scene(());
        ///Marker type for the `subject` field
        pub struct subject(());
    }
}

/// Builder for constructing an instance of this type.
pub struct SceneRevisionBuilder<
    St: scene_revision_state::State,
    S: BosStr = DefaultStr,
> {
    _state: PhantomData<fn() -> St>,
    _fields: (
        Option<Datetime>,
        Option<BlobRef<S>>,
        Option<StrongRef<S>>,
        Option<BlobRef<S>>,
    ),
    _type: PhantomData<fn() -> S>,
}

impl SceneRevision<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> SceneRevisionBuilder<scene_revision_state::Empty, DefaultStr> {
        SceneRevisionBuilder::new()
    }
}

impl<S: BosStr> SceneRevision<S> {
    /// Create a new builder for this type
    pub fn builder() -> SceneRevisionBuilder<scene_revision_state::Empty, S> {
        SceneRevisionBuilder::builder()
    }
}

impl SceneRevisionBuilder<scene_revision_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        SceneRevisionBuilder {
            _state: PhantomData,
            _fields: (None, None, None, None),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> SceneRevisionBuilder<scene_revision_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        SceneRevisionBuilder {
            _state: PhantomData,
            _fields: (None, None, None, None),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> SceneRevisionBuilder<St, S>
where
    St: scene_revision_state::State,
    St::CreatedAt: scene_revision_state::IsUnset,
{
    /// Set the `createdAt` field (required)
    pub fn created_at(
        mut self,
        value: impl Into<Datetime>,
    ) -> SceneRevisionBuilder<scene_revision_state::SetCreatedAt<St>, S> {
        self._fields.0 = Option::Some(value.into());
        SceneRevisionBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> SceneRevisionBuilder<St, S>
where
    St: scene_revision_state::State,
    St::Scene: scene_revision_state::IsUnset,
{
    /// Set the `scene` field (required)
    pub fn scene(
        mut self,
        value: impl Into<BlobRef<S>>,
    ) -> SceneRevisionBuilder<scene_revision_state::SetScene<St>, S> {
        self._fields.1 = Option::Some(value.into());
        SceneRevisionBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> SceneRevisionBuilder<St, S>
where
    St: scene_revision_state::State,
    St::Subject: scene_revision_state::IsUnset,
{
    /// Set the `subject` field (required)
    pub fn subject(
        mut self,
        value: impl Into<StrongRef<S>>,
    ) -> SceneRevisionBuilder<scene_revision_state::SetSubject<St>, S> {
        self._fields.2 = Option::Some(value.into());
        SceneRevisionBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St: scene_revision_state::State, S: BosStr> SceneRevisionBuilder<St, S> {
    /// Set the `thumb` field (optional)
    pub fn thumb(mut self, value: impl Into<Option<BlobRef<S>>>) -> Self {
        self._fields.3 = value.into();
        self
    }
    /// Set the `thumb` field to an Option value (optional)
    pub fn maybe_thumb(mut self, value: Option<BlobRef<S>>) -> Self {
        self._fields.3 = value;
        self
    }
}

impl<St, S: BosStr> SceneRevisionBuilder<St, S>
where
    St: scene_revision_state::State,
    St::CreatedAt: scene_revision_state::IsSet,
    St::Scene: scene_revision_state::IsSet,
    St::Subject: scene_revision_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> SceneRevision<S> {
        SceneRevision {
            created_at: self._fields.0.unwrap(),
            scene: self._fields.1.unwrap(),
            subject: self._fields.2.unwrap(),
            thumb: self._fields.3,
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(
        self,
        extra_data: BTreeMap<SmolStr, Data<S>>,
    ) -> SceneRevision<S> {
        SceneRevision {
            created_at: self._fields.0.unwrap(),
            scene: self._fields.1.unwrap(),
            subject: self._fields.2.unwrap(),
            thumb: self._fields.3,
            extra_data: Some(extra_data),
        }
    }
}

fn lexicon_doc_app_lexidraw_sceneRevision() -> LexiconDoc<'static> {
    #[allow(unused_imports)]
    use jacquard_common::{CowStr, deps::smol_str::SmolStr, types::blob::MimeType};
    use jacquard_lexicon::lexicon::*;
    use alloc::collections::BTreeMap;
    LexiconDoc {
        lexicon: Lexicon::Lexicon1,
        id: CowStr::new_static("app.lexidraw.sceneRevision"),
        defs: {
            let mut map = BTreeMap::new();
            map.insert(
                SmolStr::new_static("main"),
                LexUserType::Record(LexRecord {
                    description: Some(
                        CowStr::new_static(
                            "An immutable checkpoint of a app.lexidraw.scene record. Captured periodically alongside the mutable scene record; the scene blob is content-addressed and shared with the scene record, so no data is duplicated.",
                        ),
                    ),
                    key: Some(CowStr::new_static("tid")),
                    record: LexRecordRecord::Object(LexObject {
                        required: Some(
                            vec![
                                SmolStr::new_static("subject"),
                                SmolStr::new_static("scene"),
                                SmolStr::new_static("createdAt")
                            ],
                        ),
                        properties: {
                            #[allow(unused_mut)]
                            let mut map = BTreeMap::new();
                            map.insert(
                                SmolStr::new_static("createdAt"),
                                LexObjectProperty::String(LexString {
                                    format: Some(LexStringFormat::Datetime),
                                    ..Default::default()
                                }),
                            );
                            map.insert(
                                SmolStr::new_static("scene"),
                                LexObjectProperty::Blob(LexBlob { ..Default::default() }),
                            );
                            map.insert(
                                SmolStr::new_static("subject"),
                                LexObjectProperty::Ref(LexRef {
                                    r#ref: CowStr::new_static("com.atproto.repo.strongRef"),
                                    ..Default::default()
                                }),
                            );
                            map.insert(
                                SmolStr::new_static("thumb"),
                                LexObjectProperty::Blob(LexBlob { ..Default::default() }),
                            );
                            map
                        },
                        ..Default::default()
                    }),
                    ..Default::default()
                }),
            );
            map
        },
        ..Default::default()
    }
}