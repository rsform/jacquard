// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: place.stream.playback.whep
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::deps::bytes::Bytes;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::value::Data;
use jacquard_common::{BosStr, CowStr, DefaultStr, FromStaticStr};
use jacquard_derive::{IntoStatic, open_union};
use serde::{Deserialize, Serialize};

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct WhepParams<S: BosStr = DefaultStr> {
    pub rendition: S,
    pub streamer: S,
}

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase")]
pub struct Whep {
    pub body: Bytes,
}

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase")]
pub struct WhepOutput {
    pub body: Bytes,
}

#[derive(
    Serialize, Deserialize, Debug, Clone, PartialEq, Eq, thiserror::Error, miette::Diagnostic,
)]
#[serde(tag = "error", content = "message")]
pub enum WhepError {
    /// This user may not play this stream.
    #[serde(rename = "Unauthorized")]
    Unauthorized(Option<SmolStr>),
    /// Catch-all for unknown error codes.
    #[serde(untagged)]
    Other {
        error: SmolStr,
        message: Option<SmolStr>,
    },
}

impl core::fmt::Display for WhepError {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::Unauthorized(msg) => {
                write!(f, "Unauthorized")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::Other { error, message } => {
                write!(f, "{}", error)?;
                if let Some(msg) = message {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
        }
    }
}

/** Response marker for the `place.stream.playback.whep` procedure.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `WhepOutput` for this endpoint.*/
pub struct WhepResponse;
impl jacquard_common::xrpc::XrpcResp for WhepResponse {
    const NSID: &'static str = "place.stream.playback.whep";
    const ENCODING: &'static str = "*/*";
    type Output<S: BosStr> = WhepOutput;
    type Err = WhepError;
    fn encode_output<S: BosStr>(
        output: &Self::Output<S>,
    ) -> Result<Vec<u8>, jacquard_common::xrpc::EncodeError>
    where
        Self::Output<S>: Serialize,
    {
        Ok(output.body.to_vec())
    }
    fn decode_output<'de, S>(
        body: &'de [u8],
    ) -> Result<Self::Output<S>, jacquard_common::error::DecodeError>
    where
        S: BosStr + Deserialize<'de>,
        Self::Output<S>: Deserialize<'de>,
    {
        Ok(WhepOutput {
            body: jacquard_common::deps::bytes::Bytes::copy_from_slice(body),
        })
    }
}

impl jacquard_common::xrpc::XrpcRequest for Whep {
    const NSID: &'static str = "place.stream.playback.whep";
    const METHOD: jacquard_common::xrpc::XrpcMethod =
        jacquard_common::xrpc::XrpcMethod::Procedure("*/*");
    type Response = WhepResponse;
    fn encode_body(&self, buffer: &mut Vec<u8>) -> Result<(), jacquard_common::xrpc::EncodeError>
    where
        Self: Serialize,
    {
        Ok(buffer.copy_from_slice(self.body.as_ref()))
    }
    fn decode_body<'de>(body: &'de [u8]) -> Result<Self, jacquard_common::error::DecodeError>
    where
        Self: Deserialize<'de>,
    {
        Ok(Self {
            body: jacquard_common::deps::bytes::Bytes::copy_from_slice(body),
        })
    }
}

/** Endpoint marker for the `place.stream.playback.whep` procedure.

Path: `/xrpc/place.stream.playback.whep`. The request payload type is `Whep`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct WhepRequest;
impl jacquard_common::xrpc::XrpcEndpoint for WhepRequest {
    const PATH: &'static str = "/xrpc/place.stream.playback.whep";
    const METHOD: jacquard_common::xrpc::XrpcMethod =
        jacquard_common::xrpc::XrpcMethod::Procedure("*/*");
    type Request<S: BosStr> = Whep;
    type Response = WhepResponse;
}

pub mod whep_params_state {

    pub use crate::builder_types::{IsSet, IsUnset, Set, Unset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Rendition;
        type Streamer;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Rendition = Unset;
        type Streamer = Unset;
    }
    ///State transition - sets the `rendition` field to Set
    pub struct SetRendition<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetRendition<St> {}
    impl<St: State> State for SetRendition<St> {
        type Rendition = Set<members::rendition>;
        type Streamer = St::Streamer;
    }
    ///State transition - sets the `streamer` field to Set
    pub struct SetStreamer<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetStreamer<St> {}
    impl<St: State> State for SetStreamer<St> {
        type Rendition = St::Rendition;
        type Streamer = Set<members::streamer>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `rendition` field
        pub struct rendition(());
        ///Marker type for the `streamer` field
        pub struct streamer(());
    }
}

/// Builder for constructing an instance of this type.
pub struct WhepParamsBuilder<St: whep_params_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<S>, Option<S>),
    _type: PhantomData<fn() -> S>,
}

impl WhepParams<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> WhepParamsBuilder<whep_params_state::Empty, DefaultStr> {
        WhepParamsBuilder::new()
    }
}

impl<S: BosStr> WhepParams<S> {
    /// Create a new builder for this type
    pub fn builder() -> WhepParamsBuilder<whep_params_state::Empty, S> {
        WhepParamsBuilder::builder()
    }
}

impl WhepParamsBuilder<whep_params_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        WhepParamsBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> WhepParamsBuilder<whep_params_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        WhepParamsBuilder {
            _state: PhantomData,
            _fields: (None, None),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> WhepParamsBuilder<St, S>
where
    St: whep_params_state::State,
    St::Rendition: whep_params_state::IsUnset,
{
    /// Set the `rendition` field (required)
    pub fn rendition(
        mut self,
        value: impl Into<S>,
    ) -> WhepParamsBuilder<whep_params_state::SetRendition<St>, S> {
        self._fields.0 = Option::Some(value.into());
        WhepParamsBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> WhepParamsBuilder<St, S>
where
    St: whep_params_state::State,
    St::Streamer: whep_params_state::IsUnset,
{
    /// Set the `streamer` field (required)
    pub fn streamer(
        mut self,
        value: impl Into<S>,
    ) -> WhepParamsBuilder<whep_params_state::SetStreamer<St>, S> {
        self._fields.1 = Option::Some(value.into());
        WhepParamsBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> WhepParamsBuilder<St, S>
where
    St: whep_params_state::State,
    St::Rendition: whep_params_state::IsSet,
    St::Streamer: whep_params_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> WhepParams<S> {
        WhepParams {
            rendition: self._fields.0.unwrap(),
            streamer: self._fields.1.unwrap(),
        }
    }
}
