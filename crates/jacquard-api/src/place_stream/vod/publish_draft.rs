// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: place.stream.vod.publishDraft
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{CowStr, BosStr, DefaultStr, FromStaticStr};
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::string::{AtUri, Cid};
use jacquard_common::types::value::Data;
use jacquard_derive::{IntoStatic, open_union};
use serde::{Serialize, Deserialize};

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic, Default)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct PublishDraft<S: BosStr = DefaultStr> {
    ///The ats:// URI of the draft to publish. Must have status 'ready'.
    pub uri: S,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}


#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct PublishDraftOutput<S: BosStr = DefaultStr> {
    ///CID of the created place.stream.video record.
    pub video_cid: Cid<S>,
    ///AT-URI of the created place.stream.video record.
    pub video_uri: AtUri<S>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}


#[derive(
    Serialize,
    Deserialize,
    Debug,
    Clone,
    PartialEq,
    Eq,
    thiserror::Error,
    miette::Diagnostic
)]

#[serde(tag = "error", content = "message")]
pub enum PublishDraftError {
    /// No draft exists with the given URI for the authenticated user.
    #[serde(rename = "NotFound")]
    NotFound(Option<SmolStr>),
    /// The draft is not ready to publish (status is 'processing' or 'error').
    #[serde(rename = "NotReady")]
    NotReady(Option<SmolStr>),
    /// Catch-all for unknown error codes.
    #[serde(untagged)]
    Other { error: SmolStr, message: Option<SmolStr> },
}

impl core::fmt::Display for PublishDraftError {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::NotFound(msg) => {
                write!(f, "NotFound")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::NotReady(msg) => {
                write!(f, "NotReady")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::Other { error, message } => {
                write!(f, "{}", error)?;
                if let Some(msg) = message {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
        }
    }
}

/** Response marker for the `place.stream.vod.publishDraft` procedure.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `PublishDraftOutput<S>` for this endpoint.*/
pub struct PublishDraftResponse;
impl jacquard_common::xrpc::XrpcResp for PublishDraftResponse {
    const NSID: &'static str = "place.stream.vod.publishDraft";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = PublishDraftOutput<S>;
    type Err = PublishDraftError;
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for PublishDraft<S> {
    const NSID: &'static str = "place.stream.vod.publishDraft";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Procedure(
        "application/json",
    );
    type Response = PublishDraftResponse;
}

/** Endpoint marker for the `place.stream.vod.publishDraft` procedure.

Path: `/xrpc/place.stream.vod.publishDraft`. The request payload type is `PublishDraft<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct PublishDraftRequest;
impl jacquard_common::xrpc::XrpcEndpoint for PublishDraftRequest {
    const PATH: &'static str = "/xrpc/place.stream.vod.publishDraft";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Procedure(
        "application/json",
    );
    type Request<S: BosStr> = PublishDraft<S>;
    type Response = PublishDraftResponse;
}