// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: place.stream.media.finalizeLivestream
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{CowStr, BosStr, DefaultStr, FromStaticStr};
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::string::AtUri;
use jacquard_common::types::value::Data;
use jacquard_derive::{IntoStatic, open_union};
use serde::{Serialize, Deserialize};

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct FinalizeLivestream<S: BosStr = DefaultStr> {
    ///AT-URI of the place.stream.livestream record to finalize into a VOD. Must belong to the authenticated user.
    pub livestream: AtUri<S>,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}


#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic, Default)]
#[serde(rename_all = "camelCase", bound(deserialize = "S: Deserialize<'de> + BosStr"))]
pub struct FinalizeLivestreamOutput<S: BosStr = DefaultStr> {
    ///The ats:// URI of the draft VOD created for this finalize. The draft reaches status 'ready' when processing completes; the user publishes it from the Drafts tab via place.stream.vod.publishDraft.
    pub draft_uri: S,
    ///Identifier for the finalize job. Retained for backwards compatibility; the draft flow no longer requires the client to poll getUploadStatus.
    pub upload_id: S,
    #[serde(flatten, default, skip_serializing_if = "Option::is_none")]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}


#[derive(
    Serialize,
    Deserialize,
    Debug,
    Clone,
    PartialEq,
    Eq,
    thiserror::Error,
    miette::Diagnostic
)]

#[serde(tag = "error", content = "message")]
pub enum FinalizeLivestreamError {
    /// No livestream with the given URI is known, or it does not belong to the authenticated user.
    #[serde(rename = "LivestreamNotFound")]
    LivestreamNotFound(Option<SmolStr>),
    /// The livestream has no recorded MUXL segments to finalize (recording was not enabled, or none completed).
    #[serde(rename = "NoRecording")]
    NoRecording(Option<SmolStr>),
    /// Catch-all for unknown error codes.
    #[serde(untagged)]
    Other { error: SmolStr, message: Option<SmolStr> },
}

impl core::fmt::Display for FinalizeLivestreamError {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::LivestreamNotFound(msg) => {
                write!(f, "LivestreamNotFound")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::NoRecording(msg) => {
                write!(f, "NoRecording")?;
                if let Some(msg) = msg {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
            Self::Other { error, message } => {
                write!(f, "{}", error)?;
                if let Some(msg) = message {
                    write!(f, ": {}", msg)?;
                }
                Ok(())
            }
        }
    }
}

/** Response marker for the `place.stream.media.finalizeLivestream` procedure.

Implements `jacquard_common::xrpc::XrpcResp`; successful bodies decode as `Self::Output<S>`, which is `FinalizeLivestreamOutput<S>` for this endpoint.*/
pub struct FinalizeLivestreamResponse;
impl jacquard_common::xrpc::XrpcResp for FinalizeLivestreamResponse {
    const NSID: &'static str = "place.stream.media.finalizeLivestream";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = FinalizeLivestreamOutput<S>;
    type Err = FinalizeLivestreamError;
}

impl<S: BosStr> jacquard_common::xrpc::XrpcRequest for FinalizeLivestream<S> {
    const NSID: &'static str = "place.stream.media.finalizeLivestream";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Procedure(
        "application/json",
    );
    type Response = FinalizeLivestreamResponse;
}

/** Endpoint marker for the `place.stream.media.finalizeLivestream` procedure.

Path: `/xrpc/place.stream.media.finalizeLivestream`. The request payload type is `FinalizeLivestream<S>`; send that request with `jacquard::Client` or use this marker through lower-level `XrpcEndpoint` APIs.*/
pub struct FinalizeLivestreamRequest;
impl jacquard_common::xrpc::XrpcEndpoint for FinalizeLivestreamRequest {
    const PATH: &'static str = "/xrpc/place.stream.media.finalizeLivestream";
    const METHOD: jacquard_common::xrpc::XrpcMethod = jacquard_common::xrpc::XrpcMethod::Procedure(
        "application/json",
    );
    type Request<S: BosStr> = FinalizeLivestream<S>;
    type Response = FinalizeLivestreamResponse;
}

pub mod finalize_livestream_state {

    pub use crate::builder_types::{Set, Unset, IsSet, IsUnset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Livestream;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Livestream = Unset;
    }
    ///State transition - sets the `livestream` field to Set
    pub struct SetLivestream<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetLivestream<St> {}
    impl<St: State> State for SetLivestream<St> {
        type Livestream = Set<members::livestream>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `livestream` field
        pub struct livestream(());
    }
}

/// Builder for constructing an instance of this type.
pub struct FinalizeLivestreamBuilder<
    St: finalize_livestream_state::State,
    S: BosStr = DefaultStr,
> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<AtUri<S>>,),
    _type: PhantomData<fn() -> S>,
}

impl FinalizeLivestream<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> FinalizeLivestreamBuilder<
        finalize_livestream_state::Empty,
        DefaultStr,
    > {
        FinalizeLivestreamBuilder::new()
    }
}

impl<S: BosStr> FinalizeLivestream<S> {
    /// Create a new builder for this type
    pub fn builder() -> FinalizeLivestreamBuilder<finalize_livestream_state::Empty, S> {
        FinalizeLivestreamBuilder::builder()
    }
}

impl FinalizeLivestreamBuilder<finalize_livestream_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        FinalizeLivestreamBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> FinalizeLivestreamBuilder<finalize_livestream_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        FinalizeLivestreamBuilder {
            _state: PhantomData,
            _fields: (None,),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> FinalizeLivestreamBuilder<St, S>
where
    St: finalize_livestream_state::State,
    St::Livestream: finalize_livestream_state::IsUnset,
{
    /// Set the `livestream` field (required)
    pub fn livestream(
        mut self,
        value: impl Into<AtUri<S>>,
    ) -> FinalizeLivestreamBuilder<finalize_livestream_state::SetLivestream<St>, S> {
        self._fields.0 = Option::Some(value.into());
        FinalizeLivestreamBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> FinalizeLivestreamBuilder<St, S>
where
    St: finalize_livestream_state::State,
    St::Livestream: finalize_livestream_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> FinalizeLivestream<S> {
        FinalizeLivestream {
            livestream: self._fields.0.unwrap(),
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(
        self,
        extra_data: BTreeMap<SmolStr, Data<S>>,
    ) -> FinalizeLivestream<S> {
        FinalizeLivestream {
            livestream: self._fields.0.unwrap(),
            extra_data: Some(extra_data),
        }
    }
}