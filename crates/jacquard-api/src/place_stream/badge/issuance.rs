// @generated by jacquard-lexicon. DO NOT EDIT.
//
// Lexicon: place.stream.badge.issuance
//
// This file was automatically generated from Lexicon schemas.
// Any manual changes will be overwritten on the next regeneration.

#[allow(unused_imports)]
use alloc::collections::BTreeMap;

#[allow(unused_imports)]
use core::marker::PhantomData;
use jacquard_common::{BosStr, CowStr, DefaultStr, FromStaticStr};

#[allow(unused_imports)]
use jacquard_common::deps::codegen::unicode_segmentation::UnicodeSegmentation;
use jacquard_common::deps::smol_str::SmolStr;
use jacquard_common::types::collection::{Collection, RecordError};
use jacquard_common::types::string::{AtUri, Cid, Datetime, Did};
use jacquard_common::types::uri::{RecordUri, UriError};
use jacquard_common::types::value::Data;
use jacquard_common::xrpc::XrpcResp;
use jacquard_derive::{IntoStatic, lexicon};
use jacquard_lexicon::lexicon::LexiconDoc;
use jacquard_lexicon::schema::LexiconSchema;

use crate::com_atproto::repo::strong_ref::StrongRef;
#[allow(unused_imports)]
use jacquard_lexicon::validation::{ConstraintError, ValidationPath};
use serde::{Deserialize, Serialize};
/// Grants a specific badge to a recipient. The badge only appears in chat after the recipient adds this record to their place.stream.chat.profile selection array.

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(
    rename_all = "camelCase",
    rename = "place.stream.badge.issuance",
    tag = "$type",
    bound(deserialize = "S: Deserialize<'de> + BosStr")
)]
pub struct Issuance<S: BosStr = DefaultStr> {
    /// Strong reference to the place.stream.badge.def record being granted.
    pub badge: StrongRef<S>,
    /// Client-declared timestamp when this issuance was created.
    pub created_at: Datetime,
    /// The DID of the user being granted the badge.
    pub did: Did<S>,
    #[serde(
        flatten,
        default,
        deserialize_with = "deserialize_issuance_extra_data",
        skip_serializing_if = "Option::is_none"
    )]
    pub extra_data: Option<BTreeMap<SmolStr, Data<S>>>,
}

/// Typed wrapper for GetRecord response with this collection's record type.

#[derive(Serialize, Deserialize, Debug, Clone, PartialEq, Eq, IntoStatic)]
#[serde(rename_all = "camelCase")]
pub struct IssuanceGetRecordOutput<S: BosStr = DefaultStr> {
    #[serde(skip_serializing_if = "Option::is_none")]
    pub cid: Option<Cid<S>>,
    pub uri: AtUri<S>,
    pub value: Issuance<S>,
}

impl<S: BosStr> Issuance<S> {
    pub fn uri(uri: S) -> Result<RecordUri<S, IssuanceRecord>, UriError> {
        RecordUri::try_from_uri(AtUri::new(uri)?)
    }
}

/// Marker type for deserializing records from this collection.

#[derive(Debug, Serialize, Deserialize)]
pub struct IssuanceRecord;
impl XrpcResp for IssuanceRecord {
    const NSID: &'static str = "place.stream.badge.issuance";
    const ENCODING: &'static str = "application/json";
    type Output<S: BosStr> = IssuanceGetRecordOutput<S>;
    type Err = RecordError;
}

impl<S: BosStr> From<IssuanceGetRecordOutput<S>> for Issuance<S> {
    fn from(output: IssuanceGetRecordOutput<S>) -> Self {
        output.value
    }
}

impl<S: BosStr> Collection for Issuance<S> {
    const NSID: &'static str = "place.stream.badge.issuance";
    type Record = IssuanceRecord;
}

impl Collection for IssuanceRecord {
    const NSID: &'static str = "place.stream.badge.issuance";
    type Record = IssuanceRecord;
}

impl<S: BosStr> LexiconSchema for Issuance<S> {
    fn nsid() -> &'static str {
        "place.stream.badge.issuance"
    }
    fn def_name() -> &'static str {
        "main"
    }
    fn lexicon_doc() -> LexiconDoc<'static> {
        lexicon_doc_place_stream_badge_issuance()
    }
    fn validate(&self) -> Result<(), ConstraintError> {
        Ok(())
    }
}

fn deserialize_issuance_extra_data<'de, S, D>(
    deserializer: D,
) -> Result<Option<BTreeMap<SmolStr, Data<S>>>, D::Error>
where
    S: BosStr + serde::Deserialize<'de>,
    D: serde::Deserializer<'de>,
{
    let mut data =
        <Option<BTreeMap<SmolStr, Data<S>>> as serde::Deserialize<'de>>::deserialize(deserializer)?;
    if let Some(extra_data) = &mut data {
        extra_data.remove("$type");
        if extra_data.is_empty() {
            data = None;
        }
    }
    Ok(data)
}

pub mod issuance_state {

    pub use crate::builder_types::{IsSet, IsUnset, Set, Unset};
    #[allow(unused)]
    use ::core::marker::PhantomData;
    mod sealed {
        pub trait Sealed {}
    }
    /// State trait tracking which required fields have been set
    pub trait State: sealed::Sealed {
        type Badge;
        type CreatedAt;
        type Did;
    }
    /// Empty state - all required fields are unset
    pub struct Empty(());
    impl sealed::Sealed for Empty {}
    impl State for Empty {
        type Badge = Unset;
        type CreatedAt = Unset;
        type Did = Unset;
    }
    ///State transition - sets the `badge` field to Set
    pub struct SetBadge<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetBadge<St> {}
    impl<St: State> State for SetBadge<St> {
        type Badge = Set<members::badge>;
        type CreatedAt = St::CreatedAt;
        type Did = St::Did;
    }
    ///State transition - sets the `created_at` field to Set
    pub struct SetCreatedAt<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetCreatedAt<St> {}
    impl<St: State> State for SetCreatedAt<St> {
        type Badge = St::Badge;
        type CreatedAt = Set<members::created_at>;
        type Did = St::Did;
    }
    ///State transition - sets the `did` field to Set
    pub struct SetDid<St: State = Empty>(PhantomData<fn() -> St>);
    impl<St: State> sealed::Sealed for SetDid<St> {}
    impl<St: State> State for SetDid<St> {
        type Badge = St::Badge;
        type CreatedAt = St::CreatedAt;
        type Did = Set<members::did>;
    }
    /// Marker types for field names
    #[allow(non_camel_case_types)]
    pub mod members {
        ///Marker type for the `badge` field
        pub struct badge(());
        ///Marker type for the `created_at` field
        pub struct created_at(());
        ///Marker type for the `did` field
        pub struct did(());
    }
}

/// Builder for constructing an instance of this type.
pub struct IssuanceBuilder<St: issuance_state::State, S: BosStr = DefaultStr> {
    _state: PhantomData<fn() -> St>,
    _fields: (Option<StrongRef<S>>, Option<Datetime>, Option<Did<S>>),
    _type: PhantomData<fn() -> S>,
}

impl Issuance<DefaultStr> {
    /// Create a new builder for this type, using the default string type (DefaultStr = SmolStr) if needed
    pub fn new() -> IssuanceBuilder<issuance_state::Empty, DefaultStr> {
        IssuanceBuilder::new()
    }
}

impl<S: BosStr> Issuance<S> {
    /// Create a new builder for this type
    pub fn builder() -> IssuanceBuilder<issuance_state::Empty, S> {
        IssuanceBuilder::builder()
    }
}

impl IssuanceBuilder<issuance_state::Empty, DefaultStr> {
    /// Create a new builder with all fields unset, using the default string type, if needed
    pub fn new() -> Self {
        IssuanceBuilder {
            _state: PhantomData,
            _fields: (None, None, None),
            _type: PhantomData,
        }
    }
}

impl<S: BosStr> IssuanceBuilder<issuance_state::Empty, S> {
    /// Create a new builder with all fields unset
    pub fn builder() -> Self {
        IssuanceBuilder {
            _state: PhantomData,
            _fields: (None, None, None),
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> IssuanceBuilder<St, S>
where
    St: issuance_state::State,
    St::Badge: issuance_state::IsUnset,
{
    /// Set the `badge` field (required)
    pub fn badge(
        mut self,
        value: impl Into<StrongRef<S>>,
    ) -> IssuanceBuilder<issuance_state::SetBadge<St>, S> {
        self._fields.0 = Option::Some(value.into());
        IssuanceBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> IssuanceBuilder<St, S>
where
    St: issuance_state::State,
    St::CreatedAt: issuance_state::IsUnset,
{
    /// Set the `createdAt` field (required)
    pub fn created_at(
        mut self,
        value: impl Into<Datetime>,
    ) -> IssuanceBuilder<issuance_state::SetCreatedAt<St>, S> {
        self._fields.1 = Option::Some(value.into());
        IssuanceBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> IssuanceBuilder<St, S>
where
    St: issuance_state::State,
    St::Did: issuance_state::IsUnset,
{
    /// Set the `did` field (required)
    pub fn did(
        mut self,
        value: impl Into<Did<S>>,
    ) -> IssuanceBuilder<issuance_state::SetDid<St>, S> {
        self._fields.2 = Option::Some(value.into());
        IssuanceBuilder {
            _state: PhantomData,
            _fields: self._fields,
            _type: PhantomData,
        }
    }
}

impl<St, S: BosStr> IssuanceBuilder<St, S>
where
    St: issuance_state::State,
    St::Badge: issuance_state::IsSet,
    St::CreatedAt: issuance_state::IsSet,
    St::Did: issuance_state::IsSet,
{
    /// Build the final struct.
    pub fn build(self) -> Issuance<S> {
        Issuance {
            badge: self._fields.0.unwrap(),
            created_at: self._fields.1.unwrap(),
            did: self._fields.2.unwrap(),
            extra_data: Default::default(),
        }
    }
    /// Build the final struct with custom extra_data.
    pub fn build_with_data(self, extra_data: BTreeMap<SmolStr, Data<S>>) -> Issuance<S> {
        Issuance {
            badge: self._fields.0.unwrap(),
            created_at: self._fields.1.unwrap(),
            did: self._fields.2.unwrap(),
            extra_data: Some(extra_data),
        }
    }
}

fn lexicon_doc_place_stream_badge_issuance() -> LexiconDoc<'static> {
    use alloc::collections::BTreeMap;
    #[allow(unused_imports)]
    use jacquard_common::{CowStr, deps::smol_str::SmolStr, types::blob::MimeType};
    use jacquard_lexicon::lexicon::*;
    LexiconDoc {
        lexicon: Lexicon::Lexicon1,
        id: CowStr::new_static("place.stream.badge.issuance"),
        defs: {
            let mut map = BTreeMap::new();
            map.insert(
                SmolStr::new_static("main"),
                LexUserType::Record(LexRecord {
                    description: Some(
                        CowStr::new_static(
                            "Grants a specific badge to a recipient. The badge only appears in chat after the recipient adds this record to their place.stream.chat.profile selection array.",
                        ),
                    ),
                    key: Some(CowStr::new_static("tid")),
                    record: LexRecordRecord::Object(LexObject {
                        required: Some(
                            vec![
                                SmolStr::new_static("did"), SmolStr::new_static("badge"),
                                SmolStr::new_static("createdAt")
                            ],
                        ),
                        properties: {
                            #[allow(unused_mut)]
                            let mut map = BTreeMap::new();
                            map.insert(
                                SmolStr::new_static("badge"),
                                LexObjectProperty::Ref(LexRef {
                                    r#ref: CowStr::new_static("com.atproto.repo.strongRef"),
                                    ..Default::default()
                                }),
                            );
                            map.insert(
                                SmolStr::new_static("createdAt"),
                                LexObjectProperty::String(LexString {
                                    description: Some(
                                        CowStr::new_static(
                                            "Client-declared timestamp when this issuance was created.",
                                        ),
                                    ),
                                    format: Some(LexStringFormat::Datetime),
                                    ..Default::default()
                                }),
                            );
                            map.insert(
                                SmolStr::new_static("did"),
                                LexObjectProperty::String(LexString {
                                    description: Some(
                                        CowStr::new_static(
                                            "The DID of the user being granted the badge.",
                                        ),
                                    ),
                                    format: Some(LexStringFormat::Did),
                                    ..Default::default()
                                }),
                            );
                            map
                        },
                        ..Default::default()
                    }),
                    ..Default::default()
                }),
            );
            map
        },
        ..Default::default()
    }
}
