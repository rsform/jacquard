{
  "lexicon": 1,
  "id": "place.wisp.fs",
  "defs": {
    "directory": {
      "type": "object",
      "required": [
        "type",
        "entries"
      ],
      "properties": {
        "entries": {
          "type": "array",
          "items": {
            "type": "ref",
            "ref": "#entry"
          },
          "maxLength": 500
        },
        "type": {
          "type": "string",
          "const": "directory"
        }
      }
    },
    "entry": {
      "type": "object",
      "required": [
        "name",
        "node"
      ],
      "properties": {
        "name": {
          "type": "string",
          "maxLength": 255
        },
        "node": {
          "type": "union",
          "refs": [
            "#file",
            "#directory",
            "#subfs"
          ]
        }
      }
    },
    "file": {
      "type": "object",
      "required": [
        "type",
        "blob"
      ],
      "properties": {
        "base64": {
          "type": "boolean",
          "description": "True if blob content is base64-encoded (used to bypass PDS content sniffing)"
        },
        "blob": {
          "type": "blob",
          "description": "Content blob ref",
          "accept": [
            "*/*"
          ],
          "maxSize": 1000000000
        },
        "encoding": {
          "type": "string",
          "description": "Content encoding (e.g., gzip for compressed files)",
          "enum": [
            "gzip"
          ]
        },
        "mimeType": {
          "type": "string",
          "description": "Original MIME type before compression"
        },
        "type": {
          "type": "string",
          "const": "file"
        }
      }
    },
    "main": {
      "type": "record",
      "description": "Virtual filesystem manifest for a Wisp site",
      "record": {
        "type": "object",
        "required": [
          "site",
          "root",
          "createdAt"
        ],
        "properties": {
          "createdAt": {
            "type": "string",
            "format": "datetime"
          },
          "fileCount": {
            "type": "integer",
            "minimum": 0,
            "maximum": 1000
          },
          "root": {
            "type": "ref",
            "ref": "#directory"
          },
          "site": {
            "type": "string"
          }
        }
      }
    },
    "subfs": {
      "type": "object",
      "required": [
        "type",
        "subject"
      ],
      "properties": {
        "flat": {
          "type": "boolean",
          "description": "If true, the subfs record's root entries are merged (flattened) into the parent directory, replacing the subfs entry. If false (default), the subfs entries are placed in a subdirectory with the subfs entry's name. Flat merging is useful for splitting large directories across multiple records while maintaining a flat structure."
        },
        "subject": {
          "type": "string",
          "description": "AT-URI pointing to a place.wisp.subfs record containing this subtree.",
          "format": "at-uri"
        },
        "type": {
          "type": "string",
          "const": "subfs"
        }
      }
    }
  }
}