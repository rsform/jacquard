{
  "lexicon": 1,
  "id": "com.atproto.simplespace.listMembers",
  "defs": {
    "main": {
      "type": "query",
      "description": "List the members in a space's host-internal member list. Must be called on the space authority's PDS. Requires OAuth with a covering read grant; a space credential is not sufficient, so members hosted elsewhere cannot enumerate the list. This reflects the simplespace member list, not a protocol-level reader set.",
      "parameters": {
        "type": "params",
        "required": [
          "space"
        ],
        "properties": {
          "cursor": {
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "description": "Maximum number of members to return.",
            "default": 100,
            "minimum": 1,
            "maximum": 1000
          },
          "space": {
            "type": "string",
            "description": "Reference to the space.",
            "format": "space-ref"
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": [
            "members"
          ],
          "properties": {
            "cursor": {
              "type": "string"
            },
            "members": {
              "type": "array",
              "items": {
                "type": "ref",
                "ref": "#member"
              }
            }
          }
        }
      },
      "errors": [
        {
          "name": "SpaceNotFound"
        }
      ]
    },
    "member": {
      "type": "object",
      "required": [
        "did"
      ],
      "properties": {
        "did": {
          "type": "string",
          "format": "did"
        }
      }
    }
  }
}